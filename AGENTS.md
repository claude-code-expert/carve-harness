# AGENTS.md — 에이전트 표준 (크로스 에이전트 가드레일)

> 이 파일은 **모든 코딩 에이전트**(Claude Code·Cursor·Codex·Aider·Gemini CLI 등)에 적용되는 공통 규칙이다.
> Claude Code는 `CLAUDE.md` + `.claude/CLAUDE.md` + PreToolUse 훅으로 이 규칙 일부를 **자동 강제**하지만,
> 훅이 없는 에이전트는 아래 규칙을 **스스로 준수**해야 한다. 규칙의 권위는 강제 수단이 아니라 이 문서에 있다.
> 스택별 세부 규칙은 `.claude/rules/`에 있다 — 해당 파일도 함께 읽어라.

---

## 0. 절대 금지 (언어·에이전트 무관, 강제)

- **시크릿 하드코딩 금지** → 환경변수/시크릿 매니저. 코드·커밋·로그·에러메시지·URL에 시크릿/PII 노출 금지.
- **위험 git 금지**: `push --force`, `reset --hard`, 히스토리 재작성, `commit --no-verify`.
- **기존 마이그레이션 수정 금지** → 새 버전만 추가(체크섬/이력 보존).
- **파괴적 DB 작업 금지**: `WHERE` 없는 `DELETE`/`UPDATE`, `DROP`/`TRUNCATE`, 볼륨 삭제 — 승인 없이 금지.
- **미검증 완료 선언 금지** → 완료는 검증 가능한 완료기준(SC)으로만 증명한다.
- `eval`/`new Function`/`exec`에 외부 입력 전달 금지, 미검증 HTML 렌더 금지(XSS).

---

## 1. 커뮤니케이션 · 에스컬레이션

**사용자 신호를 정확히 해석하라. 불필요하게 되묻지 마라.**

- 오타는 맥락으로 해석하고 되묻지 않는다.
- 사소한 분기(파일명·포맷 등)는 기본값으로 진행하고 1줄 고지한다.
- 같은 지적을 두 번 받으면 즉시 규칙으로 정착시킨다. 세 번째는 없다.
- 해석이 여럿이면 모두 제시하라 — 말없이 하나 고르지 마라.

**다음은 자율 판단 금지 → 진행 전 에스컬레이션:**
- 아키텍처 변경 · 반복 실패 · 스펙 충돌 · 비가역 작업(덮어쓰기·삭제·퍼블리시).
- 정본 소스가 불명확하거나 요구 범위가 서로 충돌할 때.
- 작업 결과가 기존 산출물을 퇴행시킬 것으로 예상될 때.
- 입력이 비어 있거나 대상이 실재하지 않을 때 → 멈추고 먼저 보고(빈 데이터 분석한 척 금지).

**에스컬레이션 포맷:**
```
[ESCALATION]
- 상황: / 막힌 지점: / 시도한 것: / 필요한 결정:
```

---

## 2. 코딩 전에 생각하라

**가정하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

- 가정을 명시하라. 확신이 없으면 물어라.
- 더 단순한 접근이 있으면 말하라. 정당한 근거가 있으면 반론하라.
- 무언가 불분명하면 멈추고 무엇이 헷갈리는지 짚어라.

---

## 3. 단순함을 우선하라 (YAGNI)

- 문제를 푸는 **최소한의 코드**. 추측성 코드 없다.
- 요청 안 한 기능·유연성·설정 가능성 넣지 마라.
- 일어날 수 없는 시나리오의 에러 처리 하지 마라.
- 200줄 썼는데 50줄로 되면 다시 써라. 기준: "시니어가 과하게 복잡하다 할까?"

---

## 4. 외과적 변경

**꼭 필요한 것만 건드려라. 네가 만든 것만 정리하라.**

- 인접 코드·주석·포맷을 "개선"하지 마라. 안 망가진 것 리팩터 금지.
- 네 방식과 달라도 기존 스타일을 따르라.
- 무관한 죽은 코드는 언급만, 삭제 금지(요청받지 않는 한).
- 변경으로 고아가 된 import/변수/함수만 정리하라.
- **지정 범위 밖 파일·섹션은 읽되 쓰지 마라.** "다른 건 건들지 말고"가 항상 암묵적으로 붙어 있다고 가정.
- **덮어쓰기 전 현재 상태를 읽어라** — 기존 삭제 내역 되살아남 방지. 부분 패치로 될 걸 전체 덮어쓰지 마라.
- 이전 버전보다 나빠졌으면 변명 없이 **즉시 롤백**.
- 판별 기준: 변경된 모든 줄은 사용자 요청으로 거슬러 올라갈 수 있어야 한다.

---

## 5. 목표 주도 실행

**성공 기준을 정의하라. 검증될 때까지 반복하라.**

- "버그 수정" → "버그를 재현하는 테스트를 쓴 뒤 통과시켜라". "검증 추가" → "잘못된 입력 테스트 쓴 뒤 통과".
- 다단계 작업엔 `단계 → 검증` 계획을 밝혀라.
- **완료 보고는 실재 검증 후에만** — 산출물을 직접 열어 내용을 확인하라. 명령 성공 ≠ 결과 정확.
- 정합성 전수 점검: 넘버링·목차·참조 축은 한 곳 고치면 연동된 전부 수정.
- 사실 검증: 리서치는 공식·최신 소스, 수치는 근거 제시, 미확인은 불확실 표시.

---

## 6. 역할 (생성/검증 분리)

- **생성(Generator)과 검증(Evaluator)은 분리**한다 (Self-Eval Blindspot 방지).
- Planner: 작업 분해·SC 정의 / Generator: 구현 / Evaluator: 타입·보안·예외·상태 검증.
- 출력은 SC 대비 자기 점검 후 제출.

---

## 7. 시각 산출물 게이트

- **금지**: 그라데이션 일체, 색 그림자, `blur>=20px`, 글래스모피즘, hover/load 장식, 무관한 배경 장식, 카드 상단 컬러바, 한쪽 테두리 radius, 이모지 불릿, 뱃지/pill 남발, 마케팅 상투어.
- **강제**: 무채색 베이스 + 액센트 1색(색은 의미에만), 그림자는 중성 회색 1단계 또는 없음, `border-radius` 0~8px, 폰트 의도적 선택, 흰 배경엔 진한 텍스트.
- 시각 작업 전 설치된 UI 스킬/린터를 먼저 발동하고, 자동 검사기가 있으면 통과할 때까지 재작성.

---

## 8. Git 워크플로

- `main` 직접 푸시 금지. feature/develop 브랜치 → PR → 사용자가 머지.
- 커밋 메시지: Conventional Commits 영어(`feat:`·`fix:`·`refactor:` …), 제목 50자 이하.
- **커밋·푸시·PR은 명시 요청 시에만** 실행.
- 버그는 증상 패치 말고 공통 경로의 근본 원인 한 곳을 고쳐라.
- 비자명 변경: 계획 제시 → 승인 → 구현. 단계별 완료 확인 후 다음 단계.

---

## 9. 스택 감지 · 도메인 규칙

- `**/*.java` → `.claude/rules/java-spring/` · `**/*.ts,tsx` → `.claude/rules/react-next/` · 공통 → `.claude/rules/common/` · 스택 표준 → `.claude/rules/code-convention/`.
- 프로젝트 도메인 불변식·금지사항은 `CLAUDE.md`에 추가한다(예: "주문 금액 음수 불가", "결제 승인 없이 배송상태 변경 금지"). 다른 에이전트는 그 규칙도 준수.

---

## 10. 언어

| 대상 | 언어 |
|------|------|
| 내부 추론·계획 | English |
| 코드·변수명·주석·로그·에러메시지 | English |
| Git 커밋 메시지 | English (Conventional Commits) |
| 사용자 응답·설명·요약·질문 | 한국어 |
| 에러 보고 | 한국어 설명 + 원문 에러는 English |

---

*출처: 이 리포의 `CLAUDE.md`(전역 가드레일) + `.claude/CLAUDE.md`(행동 지침). 세 파일은 같은 규칙을 공유한다 — 하나를 고치면 나머지도 맞춰라. Claude Code는 CLAUDE.md로 진입하고, 그 외 에이전트는 이 AGENTS.md를 정본으로 읽는다.*
</content>
