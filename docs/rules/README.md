# `docs/rules/` — 스택 규칙 상세본

`.claude/rules/`의 자동 로드 슬림본이 참조하는 근거·샘플·프로젝트 구조. **자동 로드 아님** — 슬림본이 가리키거나 필요할 때만 Read.

- `code-convention/dev-stack-*.md` (10): java-spring · react · nextjs · typescript · javascript · python · fastapi · orm · go · rust.
- 상세본은 언어팩과 함께 설치된다(팩이 경로를 지정). 슬림본↔상세본 매핑은 각 `.claude/rules/<stack>/patterns.md` 머리 주석.
