---
phase: 05-additive-hardening-drop-in-packaging
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - .gitignore
autonomous: false
requirements: [HYG-01, HYG-02, HYG-03]
must_haves:
  truths:
    - "HYG-02: `.gitignore` blocks root `.env*` (in addition to the existing `logs/`) — a root `.env`/`.env.production` is ignored; verified by `git check-ignore` (LICENSE deferred by user, noted as a follow-up not a blocker)"
    - "HYG-03: zero `docs.claude.com` in the shipped surface (`.claude/`, `docs/`, `specs/`, repo-root `*.md`) — already true; asserted, not edited. Remaining hits live only in `.planning/` internal artifacts (out of scope)"
    - "HYG-01: no dedicated `install.md` ships — the root `install.md` + `HARNESS-TEMPLATE-MANUAL.md` deletions are committed, `docs/md/` is added with the manual kept, and `docs/md/install.md` (a 9-line plugin snippet) is removed. Rely on `docs/md/HARNESS-TEMPLATE-MANUAL.md` (user-confirmed)"
    - "The HYG-01 deletions are an explicit approval checkpoint — this plan is `autonomous: false` and the deletion task is `type=manual` per `.claude/rules/safety.md` (docs deletions require approval); nothing is `git rm`'d before approval"
    - "The Phase 4 `/harness-audit` still exits 0 after the edits"
  artifacts:
    - path: ".gitignore"
      provides: "Existing `logs/` kept; adds a root `.env*` block (HYG-02)"
      contains: ".env"
  key_links: []
---

## Phase Goal

**As a** operator cloning this template, **I want** env files git-ignored and no dead/duplicate install docs, **so that** the drop-in is clean and won't leak secrets on first commit.

<sub>Derived from ROADMAP Phase 5 Goal (SC4). MVP structure: one packaging slice — the shipped file set is hygienic (env-ignored, single source of install truth). LICENSE is deferred per user.</sub>

<objective>
Finish drop-in hygiene: add a root `.env*` block to `.gitignore` (HYG-02), confirm the shipped docs already use `code.claude.com` (HYG-03 — assertion only), and — behind an approval gate — remove the install docs so only the manual remains (HYG-01, user-confirmed "delete entirely").

The load-bearing constraint is D-11: the HYG-01 deletions are doc removals, which `.claude/rules/safety.md` requires approval for. The deletion task is `type=manual`; execution pauses before any `git rm`. LICENSE is out of scope (user deferred).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-additive-hardening-drop-in-packaging/05-CONTEXT.md

<interfaces>
<!-- .gitignore current (add root .env* block; keep logs/). -->
  logs/

<!-- Working-tree state (pre-existing): root install.md + HARNESS-TEMPLATE-MANUAL.md deleted (unstaged);
     docs/md/ untracked with HARNESS-TEMPLATE-MANUAL.md, install.md (plugin snippet), harness-install-list.md. -->

<!-- HYG-03: shipped docs already use code.claude.com (docs/md/*). docs.claude.com remains only in .planning/. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HYG-02 gitignore root .env* block + HYG-03 link assertion</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (currently only `logs/` — append a root `.env*` block; keep `logs/`)
    - .planning/phases/05-additive-hardening-drop-in-packaging/05-CONTEXT.md §Decisions D-12, D-13
  </read_first>
  <behavior>
    - A root `.env` or `.env.production` is git-ignored; `logs/` still ignored.
    - `grep -rn 'docs.claude.com'` over the shipped surface (`.claude/`, `docs/`, `specs/`, repo-root `*.md`) finds nothing (HYG-03 already satisfied).
  </behavior>
  <action>
    Append to `.gitignore` (keep the existing `logs/` line): a blank line, a comment `# secrets — never commit env files (HYG-02)`, then `/.env`, `/.env.*`, and `*.env`. Do not create any `.env` file (that is separately safety-gated and not needed here). This task makes no doc edits for HYG-03 — it only asserts the shipped surface is already clean (see acceptance).
  </action>
  <acceptance_criteria>
    - test-command (HYG-02): `git check-ignore -q .env && git check-ignore -q .env.production` both succeed (exit 0); `git check-ignore -q logs/` still succeeds.
    - source: `.gitignore` contains `logs/` AND a `.env` rule.
    - test-command (HYG-03): `grep -rn 'docs.claude.com' .claude docs specs 2>/dev/null; grep -n 'docs.claude.com' ./*.md 2>/dev/null` → no output (shipped surface clean).
  </acceptance_criteria>
  <verify>
    <automated>git check-ignore -q .env && git check-ignore -q .env.production && ! grep -rq 'docs.claude.com' .claude docs specs 2>/dev/null && echo OK</automated>
  </verify>
  <done>Root `.env*` is git-ignored; the shipped docs carry no `docs.claude.com` link (HYG-02 + HYG-03).</done>
</task>

<task type="manual">
  <name>Task 2: HYG-01 — remove install docs, keep the manual (SAFETY-gated — requires user approval)</name>
  <files>install.md, HARNESS-TEMPLATE-MANUAL.md, docs/md/install.md, docs/md/HARNESS-TEMPLATE-MANUAL.md, docs/md/harness-install-list.md</files>
  <read_first>
    - .claude/rules/safety.md §"Require Explicit User Approval" (docs deletions require approval — why this task is manual)
    - .planning/phases/05-additive-hardening-drop-in-packaging/05-CONTEXT.md §Decisions D-10, D-11
  </read_first>
  <behavior>
    - After approval: root `install.md` and root `HARNESS-TEMPLATE-MANUAL.md` are no longer tracked (deletions committed).
    - `docs/md/HARNESS-TEMPLATE-MANUAL.md` and `docs/md/harness-install-list.md` are tracked (the move is formalized).
    - `docs/md/install.md` (the 9-line plugin snippet) is removed — no `install.md` ships anywhere.
  </behavior>
  <action>
    ⚠️ APPROVAL CHECKPOINT — do not delete or `git rm` anything until the user approves (`.claude/rules/safety.md`). On approval: stage the already-made root deletions (`git rm install.md HARNESS-TEMPLATE-MANUAL.md`, or `git add` the deletions); add the manual + list from the untracked `docs/md/` (`git add docs/md/HARNESS-TEMPLATE-MANUAL.md docs/md/harness-install-list.md`); delete `docs/md/install.md` from disk so it is never tracked (the plugin snippet, not real install steps). Do NOT touch `harness-install-list.md` content or the manual content — keep them as-is. LICENSE is not created (deferred).
  </action>
  <acceptance_criteria>
    - source: `git ls-files install.md HARNESS-TEMPLATE-MANUAL.md docs/md/install.md` returns nothing (no install.md ships; root manual untracked at root).
    - source: `git ls-files docs/md/HARNESS-TEMPLATE-MANUAL.md` returns the path (manual kept in docs/md/).
    - test-command: `! test -f docs/md/install.md` (plugin snippet removed from disk).
  </acceptance_criteria>
  <verify>
    <automated>! git ls-files --error-unmatch install.md 2>/dev/null && ! test -f docs/md/install.md && git ls-files --error-unmatch docs/md/HARNESS-TEMPLATE-MANUAL.md && echo OK</automated>
  </verify>
  <done>No `install.md` ships; the manual (docs/md/) is the single install reference; deletions were made only after explicit approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| `.gitignore` rule → committed secrets | A missing/weak rule lets a root `.env` be committed. |
| Doc deletion → user intent | Deleting docs is destructive; must be approved before `git rm`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | A root `.env` is committed because `.gitignore` misses it | mitigate | Root `.env*` block; proven by `git check-ignore` on `.env` + `.env.production`. |
| T-05-08 | Tampering (destructive) | Docs deleted without user intent / wrong files removed | mitigate | `type=manual` + `autonomous: false`; deletion scoped to install docs, manual kept; nothing `git rm`'d before approval (D-11). |

Residual `high` with no mitigation: none.
</threat_model>

<verification>
- `git check-ignore -q .env .env.production` → ignored; `logs/` still ignored.
- `grep -rn 'docs.claude.com' .claude docs specs ./*.md` → nothing (HYG-03).
- After approval: no `install.md` tracked or on disk; `docs/md/HARNESS-TEMPLATE-MANUAL.md` tracked.
- `CLAUDE_PROJECT_DIR="$PWD" bash .claude/hooks/harness-audit.sh` → exit 0.
</verification>

<success_criteria>
- ROADMAP SC4 (partial — LICENSE deferred): `.gitignore` blocks root `.env*`; manual links use `code.claude.com`. The LICENSE clause of SC4 is a documented follow-up (user deferred).
- HYG-01: no dedicated install.md ships; the manual is the single reference; deletions approved.
- HYG-02/03 provable by `git check-ignore` and a `docs.claude.com` grep.
</success_criteria>

<output>
Create `.planning/phases/05-additive-hardening-drop-in-packaging/05-03-SUMMARY.md` when done.
</output>
</content>
