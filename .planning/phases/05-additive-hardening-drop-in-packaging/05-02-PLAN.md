---
phase: 05-additive-hardening-drop-in-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/README.md
  - .claude/rules/java-spring/patterns.md
  - .claude/rules/react-next/patterns.md
  - .claude/skills/handoff/SKILL.md
  - .claude/skills/changelog/SKILL.md
autonomous: true
requirements: [CFG-05]
must_haves:
  truths:
    - "Every `[…내용없음]` stub placeholder is replaced with a real, usable generic default in all 5 files — `grep -rn '내용없음'` finds nothing in specs/README.md, the two patterns.md, and the two SKILL.md (SC3)"
    - "Defaults are generic template guidance (no project-specific domain rules invented); each file's existing content, frontmatter (`paths:` on the rules), and tone are preserved — only the placeholder line changes (surgical)"
    - "The Phase 4 `/harness-audit` still exits 0 after the edits (the sentinel check is HANDOFF-scoped and unaffected); no file is left empty or malformed"
  artifacts:
    - path: "specs/README.md"
      provides: "The `[초기 스펙 — 내용없음]` line replaced with real guidance on what accumulates in specs/"
      contains: "specs/"
    - path: ".claude/rules/java-spring/patterns.md"
      provides: "`[추가 규칙 — 내용없음]` replaced with a generic 'add project rules here' default; paths frontmatter + existing rules kept"
      contains: "paths:"
    - path: ".claude/skills/handoff/SKILL.md"
      provides: "`[프로젝트별 인계 항목 추가 — 내용없음]` replaced with a generic default; format section kept"
      contains: "진행 상황"
  key_links: []
---

## Phase Goal

**As a** operator dropping this template into a new project, **I want** no `[내용없음]` placeholders staring back at me, **so that** every stub carries a usable default I can extend instead of a blank.

<sub>Derived from ROADMAP Phase 5 Goal (SC3). MVP structure: one slice — fill the five remaining stubs with real generic defaults so the template ships complete.</sub>

<objective>
Replace the `[…내용없음]` placeholders (CFG-05) in the five stub files with concise, generic, immediately-usable defaults. No project-specific domain rules are invented — the defaults are template-level guidance ("add project rules below, e.g. …") that a real project extends. Each edit is surgical: only the placeholder line changes; frontmatter, existing rules, and the file's tone stay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-additive-hardening-drop-in-packaging/05-CONTEXT.md

<interfaces>
<!-- Stub placeholders to replace (one line each). -->
  specs/README.md:                       > [초기 스펙 — 내용없음]
  .claude/rules/java-spring/patterns.md: > [추가 규칙 — 내용없음]
  .claude/rules/react-next/patterns.md:  > [추가 규칙 — 내용없음]
  .claude/skills/handoff/SKILL.md:       > [프로젝트별 인계 항목 추가 — 내용없음]
  .claude/skills/changelog/SKILL.md:     > [프로젝트별 기록 규칙 — 내용없음]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the five CFG-05 stubs with generic defaults</name>
  <files>specs/README.md, .claude/rules/java-spring/patterns.md, .claude/rules/react-next/patterns.md, .claude/skills/handoff/SKILL.md, .claude/skills/changelog/SKILL.md</files>
  <read_first>
    - specs/README.md (the `[초기 스펙 — 내용없음]` line)
    - .claude/rules/java-spring/patterns.md (paths frontmatter + existing Spring rules + the `[추가 규칙 — 내용없음]` line)
    - .claude/rules/react-next/patterns.md (same shape)
    - .claude/skills/handoff/SKILL.md, .claude/skills/changelog/SKILL.md (format section + the `[프로젝트별 … — 내용없음]` line)
    - .planning/phases/05-additive-hardening-drop-in-packaging/05-CONTEXT.md §Decisions D-08, D-09
  </read_first>
  <behavior>
    - No `내용없음` remains in any of the five files.
    - Each placeholder line becomes a real, generic guidance line (Korean, matching tone); nothing else in the file changes.
    - `paths:` frontmatter on the two rules files is preserved; the skills' `## 출력 형식`/`## 항목 형식` sections are preserved.
  </behavior>
  <action>
    Replace each placeholder line (surgical single-line edits):
    - `specs/README.md`: `> [초기 스펙 — 내용없음]` → `> 새 기능은 \`specs/<기능>/\`에 SPEC → PLAN → 완료기준(SC) 순으로 쌓는다. HANDOFF.md·DECISIONS.md는 훅/스킬이 자동 생성한다.`
    - `.claude/rules/java-spring/patterns.md`: `> [추가 규칙 — 내용없음]` → `> 프로젝트별 규칙은 이 아래에 추가한다 (예: 도메인 이벤트 발행 규약, 트랜잭션 전파 정책, 공통 응답 래퍼).`
    - `.claude/rules/react-next/patterns.md`: `> [추가 규칙 — 내용없음]` → `> 프로젝트별 규칙은 이 아래에 추가한다 (예: 데이터 패칭 경계, 폼 검증 규약, 라우팅 컨벤션).`
    - `.claude/skills/handoff/SKILL.md`: `> [프로젝트별 인계 항목 추가 — 내용없음]` → `> 프로젝트별 인계 항목은 이 아래에 추가한다 (예: 배포 상태, 미해결 티켓 번호, 롤백 절차).`
    - `.claude/skills/changelog/SKILL.md`: `> [프로젝트별 기록 규칙 — 내용없음]` → `> 프로젝트별 기록 규칙은 이 아래에 추가한다 (예: ADR 번호 체계, 승인자 명시, 관련 PR 링크).`
    Change nothing else in any file.
  </action>
  <acceptance_criteria>
    - source (SC3): `grep -rn '내용없음' specs/README.md .claude/rules/java-spring/patterns.md .claude/rules/react-next/patterns.md .claude/skills/handoff/SKILL.md .claude/skills/changelog/SKILL.md` returns nothing.
    - source (surgical): the two rules files still contain their `paths:` frontmatter and prior rules; the two skills still contain their format sections.
    - behavior: `CLAUDE_PROJECT_DIR="$PWD" bash .claude/hooks/harness-audit.sh` → exit 0 (self-audit unaffected).
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq '내용없음' specs/README.md .claude/rules/java-spring/patterns.md .claude/rules/react-next/patterns.md .claude/skills/handoff/SKILL.md .claude/skills/changelog/SKILL.md && echo CLEAN</automated>
  </verify>
  <done>All five stubs carry real generic defaults; no `[내용없음]` placeholder remains where content is expected; the self-audit stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Template stub → dropped-in project | Defaults must be generic (no invented domain rules) so they don't misconfigure a real project. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | A "default" invents a project-specific rule that misleads a real project | mitigate | Defaults are explicitly "add rules here, e.g. …" guidance, not enforced rules (D-09). |
| T-05-06 | Tampering (scope) | Filling a stub accidentally alters frontmatter or existing rules | mitigate | Surgical single-line edits; source asserts `paths:` + prior content preserved. |

Residual `high` with no mitigation: none.
</threat_model>

<verification>
- `grep -rn '내용없음'` across the five files → nothing.
- The two rules files keep their `paths:` frontmatter; the two skills keep their format sections.
- `CLAUDE_PROJECT_DIR="$PWD" bash .claude/hooks/harness-audit.sh` → exit 0.
</verification>

<success_criteria>
- ROADMAP SC3 provable: no `[내용없음]` sentinel remains in specs/README, the stack-rule patterns, or the skill bodies.
- Defaults are generic and usable; existing content preserved.
</success_criteria>

<output>
Create `.planning/phases/05-additive-hardening-drop-in-packaging/05-02-SUMMARY.md` when done.
</output>
</content>
