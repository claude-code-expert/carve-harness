---
phase: 03-m10-local-effect-telemetry-opt-in
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: true
requirements: [TELEM-01, TELEM-02, TELEM-03, TELEM-04]
must_haves:
  truths:
    - "Full quality gate passes: tsc clean, all tests green, coverage >= 80"
    - "Every instrumented hook and _metrics.sh pass bash -n and the carve auditor (no ERROR findings)"
    - "Telemetry is OFF by default and emits only {ts,hook,event} — verified end-to-end"
  artifacts: []
  key_links:
    - from: "phase gate"
      to: "TELEM-01..04"
      via: "npm run check + npm test + npm run test:cov + auditor"
      pattern: "TELEM"
---

<objective>
Phase quality gate. Verify the M10 telemetry work satisfies the LOCKED guardrails end-to-end before the phase closes: opt-in default OFF, local-only, no network, redacted records, exit-2 blocking byte-identical, auditor clean, no new dependencies, coverage >= 80.

This plan writes no production code — it runs the verification battery and, only if a check fails, the executor returns to the owning plan to fix (it does not invent new behavior here).

Purpose: TELEM-01..04 consolidated verification (PROJECT.md C10 quality gate, D7/D8/C2/C9 guardrails).
Output: a passing gate recorded in the SUMMARY; no file changes expected.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

<read_first>
- src/auditor.ts — `audit()` + `auditShellSyntax()`. The gate runs the auditor over the generated artifacts (including _metrics.sh and instrumented hooks) and asserts zero ERROR findings (`errorsOf(...)`). The existing test/unit/auditor.test.ts and test/unit/assets.test.ts may already exercise the audit over assets — confirm they cover the new helper.
- package.json scripts — `check` (tsc --noEmit), `test` (node --test), `test:cov` (coverage gate >=80).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full phase quality gate</name>
  <files></files>
  <action>
Run, in order, and require each to pass:
1. `npm run check` — tsc --noEmit clean (no type errors from cmdReport / generator change).
2. `bash -n` on assets/hooks/_metrics.sh and each of the six instrumented hooks (block-destructive, protect-secrets, pre-commit-lint, pre-push-test, auto-format, anti-slop).
3. `npm test` — full unit + e2e suite, including the new metrics.test.ts, generator.test.ts additions, commands.test.ts additions, and report.e2e.test.ts. Existing hooks.test.ts exit-code assertions MUST still pass (proves exit-2 blocking byte-identical, D7).
4. `npm run test:cov` — coverage gate >= 80 (no regression; project baseline ~95).
If any step fails, do NOT patch behavior in this plan — identify the owning plan (03-01 shell/redaction, 03-02 generator, 03-03 report) and return that as the failure so the executor fixes at the source. Record the gate result in the SUMMARY.
  </action>
  <verify>
    <automated>npm run check && for h in _metrics block-destructive protect-secrets pre-commit-lint pre-push-test auto-format anti-slop; do bash -n assets/hooks/$h.sh || exit 1; done && npm test && npm run test:cov</automated>
  </verify>
  <done>tsc clean, bash -n clean on _metrics.sh + 6 hooks, full test suite green (including hooks.test.ts unchanged), coverage >= 80.</done>
</task>

<task type="auto">
  <name>Task 2: Auditor + guardrail spot-check on telemetry assets</name>
  <files></files>
  <action>
Confirm the telemetry assets pass the carve auditor and the LOCKED guardrails:
1. No-network: `grep -REn 'curl|wget|[^a-z]nc[^a-z.]' assets/hooks/_metrics.sh assets/hooks/{block-destructive,protect-secrets,pre-commit-lint,pre-push-test,auto-format,anti-slop}.sh` returns nothing (C2).
2. Auditor clean: ensure the auditor over the generated artifacts (which now include `.claude/hooks/_metrics.sh`) yields zero ERROR findings — confirm via the existing assets/auditor unit tests (or, if not covered, note it). In particular _metrics.sh must NOT trip the `hook-injection` rule (it writes `.carve-metrics.jsonl`, a data file, not settings.json or a hook file) (D8).
3. No new runtime deps: `git diff package.json` shows no dependency additions (C3); the single runtime dep stays `@clack/prompts`.
4. Opt-in default OFF confirmed by metrics.test.ts (already run in Task 1) — the opt-out case writes no jsonl.
Record findings in the SUMMARY.
  </action>
  <verify>
    <automated>! grep -REn 'curl|wget' assets/hooks/_metrics.sh assets/hooks/block-destructive.sh assets/hooks/protect-secrets.sh assets/hooks/pre-commit-lint.sh assets/hooks/pre-push-test.sh assets/hooks/auto-format.sh assets/hooks/anti-slop.sh && git diff --quiet package.json && echo OK</automated>
  </verify>
  <done>No network calls in telemetry assets; auditor yields no ERROR over the new artifacts; package.json dependencies unchanged; opt-in default OFF confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase gate → shipped telemetry behavior | final check that the additive telemetry never weakened the exit-2 boundary or opened a network/redaction hole. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | regression in any hook's exit-2 path | mitigate | Task 1 re-runs hooks.test.ts; any exit-code drift fails the gate and routes back to 03-01. |
| T-03-10 | Information disclosure | accidental network/secret introduction | mitigate | Task 2 greps for curl/wget/nc and runs the auditor; package.json diff guards against a new dep slipping in (C2/C3). |
</threat_model>

<verification>
- `npm run check` + `npm test` + `npm run test:cov` (>=80) all green.
- `bash -n` clean on _metrics.sh + 6 instrumented hooks.
- No curl/wget/nc in telemetry assets; auditor zero ERROR over new artifacts; package.json deps unchanged.
</verification>

<success_criteria>
- All four TELEM requirements verified end-to-end; phase gate green; guardrails (D7/D8/C2/C3/C9) intact.
</success_criteria>

<output>
Create `.planning/phases/03-m10-local-effect-telemetry-opt-in/03-04-SUMMARY.md` when done.
</output>
