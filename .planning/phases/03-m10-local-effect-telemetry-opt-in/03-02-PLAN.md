---
phase: 03-m10-local-effect-telemetry-opt-in
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/generator.ts
  - test/unit/generator.test.ts
autonomous: true
requirements: [TELEM-01, TELEM-02]
must_haves:
  truths:
    - "Whenever any carve hook is installed, _metrics.sh is shipped to .claude/hooks/_metrics.sh as an executable artifact"
    - "_metrics.sh is shipped verbatim (no template corruption — it has no {{VARS}})"
    - "_metrics.sh is shipped exactly once even when several hooks are recommended"
  artifacts:
    - path: "src/generator.ts"
      provides: "generate() emits .claude/hooks/_metrics.sh artifact alongside instrumented hooks"
      contains: "_metrics.sh"
  key_links:
    - from: "src/generator.ts generate()"
      to: "assets/hooks/_metrics.sh"
      via: "readAsset('hooks/_metrics.sh')"
      pattern: "_metrics.sh"
---

<objective>
Make `generate()` ship `assets/hooks/_metrics.sh` to `.claude/hooks/_metrics.sh` (executable) whenever ANY carve hook is installed, so the instrumented hooks' `source ".../_metrics.sh"` line resolves at runtime in the target project. The instrumented hook assets (from plan 03-01) already carry the source line + carve_metric calls, so no other generator change is needed beyond shipping the helper file.

Purpose: completes TELEM-01/TELEM-02 delivery into the target `.claude/` (layer B). Without this, the source line in installed hooks would no-op (it already `|| true`-guards), and telemetry would never emit even when opted in.
Output: updated `src/generator.ts` + a generator unit test asserting the helper artifact is produced.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<read_first>
- src/generator.ts — `generate(profile, design)` builds the Artifact[] list. The hook install loop (lines 150-159) iterates `design.recommended`, and for each id with an entry in `HOOK_ASSETS` (lines 84-92), pushes `{ path: .claude/hooks/carve-<id>.sh, content: render(readAsset(asset), vars), executable: true }`. `readAsset(rel)` (line 18) reads relative to `assets/`. `render(tmpl, vars)` (line 79) only replaces `{{KEY}}` tokens — since _metrics.sh has none, readAsset alone (or render with vars, harmless) is fine.
- The anti-slop block (lines 162-171) and codesight block (174-176) push additional hooks. Decide "any carve hook installed" by whether the artifacts list already contains at least one `.claude/hooks/carve-*.sh` OR `.claude/hooks/carve-anti-slop.sh` — simplest: track a boolean as you build, or check `artifacts.some(a => a.path.startsWith('.claude/hooks/'))` just before returning.
</read_first>

<interfaces>
From src/generator.ts:
```typescript
export interface Artifact { path: string; content: string; executable: boolean; }
export function readAsset(rel: string): string;
export function render(tmpl: string, vars: Record<string,string>): string;
export function generate(profile: ProjectProfile, design: HarnessDesign): Artifact[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship _metrics.sh from generate() when any hook is installed</name>
  <files>src/generator.ts</files>
  <action>
In `generate()`, after the hook artifacts are assembled (the simplest correct point is just before `return artifacts;` at line 206), conditionally push ONE `_metrics.sh` artifact:
- Condition: any hook script is in the artifact list. Compute `const anyHook = artifacts.some((a) => a.path.startsWith('.claude/hooks/'));` (covers carve-<id>.sh, carve-anti-slop.sh, carve-codesight-refresh.sh, and squad hooks). This guarantees the helper ships whenever an instrumented hook ships.
- When true, push `{ path: '.claude/hooks/_metrics.sh', content: readAsset('hooks/_metrics.sh'), executable: true }`. Use `readAsset` directly (the file has no `{{VARS}}`), or `render(readAsset('hooks/_metrics.sh'), vars)` — both are correct; prefer plain `readAsset` for clarity per the design note.
- Push it exactly ONCE (the single `if (anyHook)` guard guarantees this even with many hooks recommended).
- Korean comment per file convention. Do not change HOOK_ASSETS, HOOK_MATCHER, hookRegsFor, or the existing loop — _metrics.sh is a sourced helper, NOT a registered hook, so it must NOT appear in settings.json hook regs (do not add it to hookRegsFor).
  </action>
  <verify>
    <automated>npm run check && grep -q "_metrics.sh" src/generator.ts && ! grep -q "_metrics" <(sed -n '46,68p' src/generator.ts) && echo OK</automated>
  </verify>
  <done>generate() pushes .claude/hooks/_metrics.sh (executable) iff at least one .claude/hooks/ artifact exists; _metrics.sh is NOT added to hookRegsFor; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generator.test.ts — _metrics.sh shipped once with hooks, absent without</name>
  <files>test/unit/generator.test.ts</files>
  <behavior>
    - When a design recommending at least one hook (e.g. block-destructive) is generated, the artifact list contains exactly one `.claude/hooks/_metrics.sh` entry, it is `executable: true`, and its content contains `carve_metric` and no `{{` template token (verbatim ship).
    - The _metrics.sh path appears at most once even when multiple hooks are recommended.
    - When a design recommends NO hooks (empty/skill-only recommended set that produces no `.claude/hooks/` artifacts), no `_metrics.sh` artifact is present.
  </behavior>
  <action>
Extend (or, if it does not already exist, the executor adds to) test/unit/generator.test.ts. Reuse the existing test's pattern for constructing a `ProjectProfile` and a `HarnessDesign` (import `generate`; build a minimal `design` object with a `recommended` array and `available`/`level` as the existing tests do — read the current file first to match its fixture style). Assert with `artifacts.filter(a => a.path === '.claude/hooks/_metrics.sh')` length === 1 (hooks case) / === 0 (no-hooks case), `.executable === true`, content `includes('carve_metric')` and `!includes('{{')`. No new dependencies.
  </action>
  <verify>
    <automated>node --test test/unit/generator.test.ts</automated>
  </verify>
  <done>generator.test.ts proves _metrics.sh ships exactly once (executable, verbatim) when hooks are present and is absent when no hooks are recommended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator (layer A) → installed artifact (layer B) | generate() only reads assets and emits Artifact[]; installer writes them. _metrics.sh is shipped verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | render() corrupting _metrics.sh | mitigate | Ship via plain readAsset (no `{{VARS}}` in the file); test asserts content has no `{{` token and still contains carve_metric. |
| T-03-06 | Elevation | _metrics.sh registered as a runnable hook | mitigate | Helper is NOT added to hookRegsFor → never enters settings.json hook regs; only sourced by sibling hooks. Verify step greps the hookRegsFor region to confirm absence. |
</threat_model>

<verification>
- `npm run check` (tsc --noEmit) clean.
- `node --test test/unit/generator.test.ts` passes.
- `_metrics.sh` does not appear in `hookRegsFor` (settings.json regs) — only as a generated file artifact.
</verification>

<success_criteria>
- TELEM-01/TELEM-02 delivery: any install that includes a carve hook also ships `.claude/hooks/_metrics.sh` (executable, verbatim, once), so installed instrumented hooks can source it at runtime.
</success_criteria>

<output>
Create `.planning/phases/03-m10-local-effect-telemetry-opt-in/03-02-SUMMARY.md` when done.
</output>
