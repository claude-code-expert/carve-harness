---
phase: 03-m10-local-effect-telemetry-opt-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/hooks/_metrics.sh
  - assets/hooks/block-destructive.sh
  - assets/hooks/protect-secrets.sh
  - assets/hooks/pre-commit-lint.sh
  - assets/hooks/pre-push-test.sh
  - assets/hooks/auto-format.sh
  - assets/hooks/anti-slop.sh
  - test/unit/metrics.test.ts
autonomous: true
requirements: [TELEM-01, TELEM-02, TELEM-03]
must_haves:
  truths:
    - "With CARVE_METRICS unset (default OFF), no .carve-metrics.jsonl is written and every hook's exit code is unchanged"
    - "With CARVE_METRICS=on, a blocking hook still exits 2 AND appends exactly one {ts,hook,event} line"
    - "No emitted line ever contains a command body, path, or secret"
    - "No _metrics.sh or instrumented hook contains curl/wget/nc (no network)"
  artifacts:
    - path: "assets/hooks/_metrics.sh"
      provides: "carve_metric emit helper, opt-in gated, always returns 0"
      contains: "carve_metric"
    - path: "test/unit/metrics.test.ts"
      provides: "opt-out no-op, opt-in emit, redaction, no-network, bash -n coverage"
  key_links:
    - from: "assets/hooks/block-destructive.sh"
      to: "assets/hooks/_metrics.sh"
      via: "source line near top + carve_metric call before exit 2"
      pattern: "source .*_metrics.sh"
---

<objective>
Create the shared opt-in telemetry emit helper `assets/hooks/_metrics.sh` and instrument the six effect hooks (block-destructive, protect-secrets, pre-commit-lint, pre-push-test, auto-format, anti-slop) to call it at meaningful points. Telemetry is a PURE ADDITIVE side-effect, OFF by default. The deterministic exit-2 blocking logic of every hook stays BYTE-FOR-BYTE unchanged — only the `source` line plus `carve_metric` calls are added.

Purpose: TELEM-01 (emit helper, opt-in), TELEM-02 (hooks emit, blocking unchanged), TELEM-03 (redaction + no network). This is the foundation the generator ships and `carve report` aggregates.
Output: `assets/hooks/_metrics.sh` + 6 instrumented hooks + `test/unit/metrics.test.ts`.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<read_first>
- assets/hooks/block-destructive.sh — has `block(){ echo ... >&2; exit 2; }` (line 10) called at lines 14/22/28; final `exit 0` at line 31. cwd at runtime = target project root.
- assets/hooks/protect-secrets.sh — inline `exit 2` at line 12 (inside the secret-match `if`); final `exit 0` line 14.
- assets/hooks/pre-commit-lint.sh — non-git path early-exits at line 6 (`grep ... || exit 0`); failure `exit 2` at line 12; final `exit 0` at line 14 (reached only after lint ran successfully on a detected `git commit`).
- assets/hooks/pre-push-test.sh — same shape as pre-commit-lint: non-push early-exit line 6, fail `exit 2` line 12, final `exit 0` line 14.
- assets/hooks/auto-format.sh — non-blocking; final `exit 0` at line 11 after the `eval "$FMT ..."`.
- assets/hooks/anti-slop.sh — non-blocking warning; the slop-detected branch is the `if printf '%s' "$out" | grep -q 'ERROR'` block (lines 24-27) which prints the warning before the final `exit 0` at line 28.
- test/unit/hooks.test.ts — EXISTING exit-code + bash -n assertions for all hooks. These MUST stay green unchanged (metrics OFF by default → emit is a no-op, so exit codes are identical). Note the `runHook(name, payload, env)` helper passes env to the subprocess; mirror its shape in the new test.
- src/auditor.ts — `audit()` flags `hook-injection` ERROR when a file under `/hooks/` both matches `settings.json|.claude/hooks/` AND a write op (`>>?|tee|writeFile`). `_metrics.sh` appends to `.claude/.carve-metrics.jsonl` (NOT a hook file, NOT settings.json) so it must NOT trip this rule — keep the redirect target exactly `.claude/.carve-metrics.jsonl`. `auditShellSyntax()` runs `bash -n` (+ shellcheck -S error if present) on every `.sh`.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/hooks/_metrics.sh emit helper</name>
  <files>assets/hooks/_metrics.sh</files>
  <action>
Create the shared emit helper, sourced by instrumented hooks (TELEM-01). Requirements:
- Shebang `#!/usr/bin/env bash` and `set -uo pipefail` to match sibling hooks (but it is sourced, so it must not hard-exit the caller — see below).
- Define ONE function `carve_metric` taking two args: `<hook>` and `<event>`.
- Opt-in gate FIRST: emit only if `CARVE_METRICS=on` OR the file `.claude/.carve-metrics.enabled` exists (relative to cwd, which is the target project root). Otherwise `return 0` immediately — default OFF.
- When enabled: compute `ts` via `date +%s` with fallback `0` if it fails. Append exactly ONE line to `.claude/.carve-metrics.jsonl` using `printf` with the JSON shape `{"ts":<ts>,"hook":"<hook>","event":"<event>"}`. Record ONLY ts/hook/event (TELEM-03) — never a command, path, or secret. Create the `.claude/` dir defensively (`mkdir -p .claude 2>/dev/null`) since a hook may run before install dirs exist.
- Swallow ALL errors (`2>/dev/null` on the append) and ALWAYS `return 0` at the end so the function can NEVER alter the caller's exit code (TELEM-02 — blocking unchanged).
- NO network: the file must contain no `curl`, `wget`, or `nc`. No `{{VARS}}` template tokens (generator must be able to ship it verbatim via readAsset).
- Korean comments per project convention (this repo uses Korean comments in hooks).
Keep it minimal — one function, ~15 lines. Do not add configurability beyond the opt-in gate.
  </action>
  <verify>
    <automated>bash -n assets/hooks/_metrics.sh && grep -q 'carve_metric' assets/hooks/_metrics.sh && ! grep -Eq 'curl|wget|[^a-z]nc[^a-z]' assets/hooks/_metrics.sh && grep -q '.carve-metrics.jsonl' assets/hooks/_metrics.sh && echo OK</automated>
  </verify>
  <done>_metrics.sh defines carve_metric with an opt-in gate, appends a {ts,hook,event} JSONL line only when enabled, always returns 0, contains no network calls, passes bash -n.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument the six effect hooks with source line + carve_metric calls</name>
  <files>assets/hooks/block-destructive.sh, assets/hooks/protect-secrets.sh, assets/hooks/pre-commit-lint.sh, assets/hooks/pre-push-test.sh, assets/hooks/auto-format.sh, assets/hooks/anti-slop.sh</files>
  <action>
For EACH of the six hooks, add (a) ONE source line immediately AFTER the `set -uo pipefail` line:
`source "$(dirname "${BASH_SOURCE[0]}")/_metrics.sh" 2>/dev/null || true`
and (b) `carve_metric` calls at the points below. Do NOT move, edit, or re-order ANY existing `grep`, `printf`, `eval`, threshold, or `exit` line — only ADD lines. The existing test/unit/hooks.test.ts exit-code assertions MUST pass unchanged (metrics OFF by default makes every emit a no-op).
- block-destructive.sh: inside `block()` (line 10), add `carve_metric block-destructive block` BEFORE `exit 2` so all three block paths are covered by one call.
- protect-secrets.sh: add `carve_metric protect-secrets block` immediately before the `exit 2` at line 12 (inside the secret-match `if`).
- pre-commit-lint.sh: add `carve_metric pre-commit-lint block` before the failure `exit 2` (line 12); add `carve_metric pre-commit-lint pass` immediately before the final `exit 0` (line 14, reached only after a detected `git commit` lint ran successfully).
- pre-push-test.sh: same pattern — `carve_metric pre-push-test block` before the fail `exit 2`; `carve_metric pre-push-test pass` before the final `exit 0`.
- auto-format.sh: add `carve_metric auto-format fire` immediately before the final `exit 0` (line 11, after the formatter eval).
- anti-slop.sh: add `carve_metric anti-slop warn` inside the `if ... grep -q 'ERROR'` slop-detected block (lines 24-27), before the block closes — so it only fires when slop is actually detected. Do NOT add it to the early non-target `exit 0` paths.
Do NOT instrument slack-notify / precompact-handoff / codesight-refresh (out of scope — effect hooks only).
Critical: every existing grep condition, threshold, and exit code stays identical. After editing, `bash -n` each file and re-run hooks.test.ts to confirm exit codes are byte-identical.
  </action>
  <verify>
    <automated>for h in block-destructive protect-secrets pre-commit-lint pre-push-test auto-format anti-slop; do bash -n assets/hooks/$h.sh || exit 1; grep -q 'source .*_metrics.sh' assets/hooks/$h.sh || { echo "missing source: $h"; exit 1; }; grep -q 'carve_metric' assets/hooks/$h.sh || { echo "missing call: $h"; exit 1; }; done && node --test test/unit/hooks.test.ts && echo OK</automated>
  </verify>
  <done>All six hooks source _metrics.sh and call carve_metric at the specified points; every existing hooks.test.ts assertion passes unchanged; bash -n clean on all six.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test/unit/metrics.test.ts — opt-out no-op, opt-in emit, redaction, no-network</name>
  <files>test/unit/metrics.test.ts</files>
  <behavior>
    - Default OFF: run block-destructive.sh with a dangerous command (`rm -rf /`) in a temp cwd with CARVE_METRICS unset → exit code is 2 AND no `.claude/.carve-metrics.jsonl` is created (proves TELEM-02 blocking-unchanged + opt-out).
    - Opt-in: same dangerous command with `CARVE_METRICS=on`, cwd = temp dir → exit 2 AND `.claude/.carve-metrics.jsonl` has exactly one line; parse it as JSON → keys are exactly {ts, hook, event}, hook==='block-destructive', event==='block'; the line does NOT contain the substring `rm -rf` (redaction, TELEM-03).
    - No-network: assert _metrics.sh and each of the six instrumented hooks contain no `curl`/`wget`/`nc` (read file contents, regex assert).
    - bash -n: assert `bash -n` exits 0 for _metrics.sh and all six instrumented hooks.
  </behavior>
  <action>
Mirror the helpers in test/unit/hooks.test.ts (`hook(name)` URL resolver, `spawnSync('bash', [hook(name)], {input, env})`). To make cwd = temp dir for the opt-in/opt-out cases, pass `cwd` to spawnSync (the hooks resolve `.claude/...` relative to cwd). Use `mkdtempSync(join(tmpdir(),'carve-metrics-'))` and clean up with `rmSync(..., {recursive:true, force:true})` in a finally. Read jsonl with `readFileSync`, split on newline, filter blank, JSON.parse the single line, assert `Object.keys(obj).sort()` deep-equals `['event','hook','ts']`. For no-network/bash -n, loop over the helper-name list. Do NOT add new dependencies — node:test + node:assert/strict + node:child_process + node:fs + node:os + node:path only.
  </action>
  <verify>
    <automated>node --test test/unit/metrics.test.ts</automated>
  </verify>
  <done>metrics.test.ts passes: opt-out writes nothing while still exiting 2, opt-in writes one redacted {ts,hook,event} line, no hook contains network calls, bash -n clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code runtime → hook stdin | tool_input JSON is untrusted; hooks already parse it via node and never eval it. _metrics.sh never reads stdin. |
| hook → local filesystem (.claude/) | the only write is an append to `.claude/.carve-metrics.jsonl`. No network egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | _metrics.sh recorded line | mitigate | Record only `{ts,hook,event}`; never interpolate `$cmd`/`$path`. metrics.test.ts asserts redaction (line excludes `rm -rf`) and key set == {ts,hook,event}. |
| T-03-02 | Information disclosure | any instrumented hook | mitigate | No `curl`/`wget`/`nc` anywhere; metrics.test.ts asserts absence across helper + 6 hooks (C2 no-network). |
| T-03-03 | Tampering | block-destructive / protect-secrets exit-2 logic | mitigate | Source line + carve_metric are additive only; emit always `return 0`; existing hooks.test.ts exit-code assertions re-run and must pass byte-identically (D7). |
| T-03-04 | Elevation | _metrics.sh self-write under /hooks/ | accept | _metrics.sh writes to `.carve-metrics.jsonl` (data file), not settings.json or a hook file, so the auditor `hook-injection` rule does not (and must not) trigger; no privileged op. |
</threat_model>

<verification>
- `bash -n` clean on `_metrics.sh` and all six instrumented hooks.
- `node --test test/unit/hooks.test.ts` — all existing exit-code/syntax assertions pass unchanged.
- `node --test test/unit/metrics.test.ts` — new opt-in/opt-out/redaction/no-network assertions pass.
- Manual grep: `grep -rEn 'curl|wget' assets/hooks/_metrics.sh assets/hooks/{block-destructive,protect-secrets,pre-commit-lint,pre-push-test,auto-format,anti-slop}.sh` returns nothing.
</verification>

<success_criteria>
- TELEM-01: _metrics.sh defines carve_metric, opt-in gated (CARVE_METRICS=on OR .claude/.carve-metrics.enabled), default OFF.
- TELEM-02: every instrumented hook's exit code is byte-for-byte unchanged with metrics OFF; hooks.test.ts green.
- TELEM-03: emitted line is exactly {ts,hook,event}; no command/path/secret; no network.
</success_criteria>

<output>
Create `.planning/phases/03-m10-local-effect-telemetry-opt-in/03-01-SUMMARY.md` when done.
</output>
