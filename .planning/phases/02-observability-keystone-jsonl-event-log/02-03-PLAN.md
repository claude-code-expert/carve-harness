---
phase: 02-observability-keystone-jsonl-event-log
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .claude/hooks/stop-verify.sh
  - .claude/hooks/session-handoff.sh
  - .claude/hooks/tests/stop-verify.test.sh
  - .claude/hooks/tests/session-handoff.test.sh
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "A Stop fire appends one JSON line (decision `pass` | `fail` | `loop-yield`) and a session start/save appends one line (decision `start` | `save`) — with pretool-guard (Plan 01) and posttool-format (Plan 02) this completes the ALL-5-hook-entry-point coverage of D-03 (SC1)"
    - "The log calls never change stop-verify's block/allow exit code (2/0) or session-handoff's exit 0 — subprocess, best-effort (D-05)"
    - "stop-verify's jq-absent early exit produces no log line but preserves its exit code, and the existing `set -o pipefail` + loop-guard behavior is unchanged (accepted gap per Pitfall 3; Phase 1 regression intact)"
    - "No new runtime dependency; `bash -n` clean on both edited hooks (SC3)"
  artifacts:
    - path: ".claude/hooks/stop-verify.sh"
      provides: "Existing pipefail + loop-guard + stack-verify preserved; subprocess log calls added at loop-yield (exit 0), pass (exit 0), and fail (exit 2) points"
      contains: "log-event.sh"
    - path: ".claude/hooks/session-handoff.sh"
      provides: "Existing arg-driven start/save preserved; subprocess log calls added for SessionStart:start and PreCompact:save"
      contains: "log-event.sh"
    - path: ".claude/hooks/tests/stop-verify.test.sh"
      provides: "Existing loop-guard/jq-absent/pipefail asserts PLUS a loop-yield-logs-one-line assert; exit codes unchanged"
      contains: "loop-yield"
    - path: ".claude/hooks/tests/session-handoff.test.sh"
      provides: "Executable asserts: start logs one `start` line, save logs one `save` line and writes HANDOFF.md, both exit 0"
      contains: "log-event.sh"
  key_links:
    - from: ".claude/hooks/stop-verify.sh"
      to: ".claude/hooks/log-event.sh"
      via: "subprocess call before each literal exit"
      pattern: "bash .*log-event.sh Stop"
    - from: ".claude/hooks/session-handoff.sh"
      to: ".claude/hooks/log-event.sh"
      via: "subprocess call in each case arm"
      pattern: "bash .*log-event.sh"
---

## Phase Goal

**As a** harness operator, **I want to** have Stop-verification and session start/save also recorded in the JSONL log, **so that** all five hook entry points leave an audit trail that `/harness-audit` (Phase 4) can consume.

<sub>Derived from ROADMAP Phase 2 Goal. MVP structure: two thin end-to-end slices — a Stop fire and a session lifecycle event each produce a real, verifiable log line. This plan completes D-03's "all 5 hooks".</sub>

<objective>
Complete OBS-01's hook coverage: with pretool-guard (Plan 01) and posttool-format (Plan 02) already logging, add the remaining two entry points — stop-verify (Stop: pass/fail/loop-yield) and session-handoff (SessionStart: start / PreCompact: save) — so every one of the 5 hook entry points leaves exactly one structured line per fire (D-03).

Purpose: the keystone log is only complete when every hook fire is recorded. These are the two smallest, lowest-risk edits: each adds a bare subprocess log call before an existing literal exit. The load-bearing constraint remains D-05 — stop-verify's block (exit 2) and the loop-guard/jq-absent exits must be byte-for-byte preserved, so logging is inserted BEFORE the existing exit, never replacing it.

Output: edited `stop-verify.sh` and `session-handoff.sh` (log calls at existing exit/branch points), an extended `stop-verify.test.sh` (loop-yield now logs), and a new `session-handoff.test.sh` (start/save log + HANDOFF.md written).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-observability-keystone-jsonl-event-log/02-CONTEXT.md
@.planning/phases/02-observability-keystone-jsonl-event-log/02-RESEARCH.md
@.planning/phases/02-observability-keystone-jsonl-event-log/02-01-PLAN.md

<interfaces>
<!-- log-event.sh contract (delivered by Plan 01 — reuse, do not rebuild). -->
  bash log-event.sh Stop verify <decision> ""            decision = pass | fail | loop-yield
  bash log-event.sh SessionStart handoff start ""
  bash log-event.sh PreCompact  handoff save ""
  Best-effort subprocess; always exits 0; cannot change the caller's exit code.

Existing stop-verify.sh contract (preserve byte-for-byte — RESEARCH §"Code Examples"):
  - `set -o pipefail` (Phase 0 root fix — never remove/reorder)
  - `input=$(cat)`; GATE-01 loop guard: `stop_hook_active=true` → stderr `루프 방지` + exit 0
  - jq-absent best-effort: `! command -v jq` → stderr `jq 미설치` + exit 0 (CANNOT log here — no jq; Pitfall 3)
  - stack detect (gradle / node) → `fail` flag → final `[ "$fail" -eq 0 ] || { stderr; exit 2; }` ; else exit 0
  - invariant: block = exit 2, allow = exit 0 (MANUAL §2.2)

Existing session-handoff.sh contract (preserve — arg-driven, ignores stdin):
  - H="specs/HANDOFF.md"; `case "${1:-}"` → `start` restores handoff, `save` writes it; final `exit 0`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Stop log calls (pass/fail/loop-yield) + extend stop-verify test (GREEN)</name>
  <files>.claude/hooks/stop-verify.sh, .claude/hooks/tests/stop-verify.test.sh</files>
  <read_first>
    - .claude/hooks/stop-verify.sh (the file being edited — keep pipefail, loop-guard, jq-absent branch, and the stack-detect block byte-for-byte)
    - .claude/hooks/tests/stop-verify.test.sh (existing Phase 1 asserts — loop guard marker, jq-absent, pipefail regression — must ALL still pass)
    - .claude/hooks/log-event.sh (helper contract — Plan 01)
    - .planning/phases/02-observability-keystone-jsonl-event-log/02-RESEARCH.md §"Code Examples" (stop-verify wiring), §"Pitfall 3" (jq-absent can't log)
    - .planning/phases/02-observability-keystone-jsonl-event-log/02-CONTEXT.md §Decisions D-03, D-05
  </read_first>
  <behavior>
    - loop-yield logs one line: feeding `{"stop_hook_active":true}` with CLAUDE_PROJECT_DIR set to a mktemp dir → exit 0 (unchanged) AND the daily JSONL gains one line with `.event`==Stop, `.decision`==loop-yield.
    - exit code unchanged under log failure: with `logs` an unwritable FILE, feeding `{"stop_hook_active":true}` still exits 0.
    - Phase 1 regression: the existing three asserts (`루프 방지` marker on `stop_hook_active=true`, jq-absent `jq 미설치` + exit 0, `set -o pipefail` grep) all still pass.
    - jq-absent still logs nothing (no jq) but exits 0 (accepted gap, Pitfall 3).
    - Source: pass/fail arms carry a `log-event.sh Stop verify pass|fail` call immediately before their literal `exit 0`/`exit 2`.
  </behavior>
  <action>
    Edit stop-verify.sh surgically. Add `LOG_EVENT="$(dirname "${BASH_SOURCE[0]}")/log-event.sh"` near the top (after the shebang comment, before or alongside the existing logic — do not disturb `set -o pipefail` ordering). At the loop-guard exit (`stop_hook_active=true` branch, currently stderr + exit 0) insert `bash "$LOG_EVENT" Stop verify loop-yield ""` immediately before the existing `exit 0`. At the final decision: the fail path (`[ "$fail" -eq 0 ] || { echo ...; exit 2; }`) becomes `... || { echo ...; bash "$LOG_EVENT" Stop verify fail ""; exit 2; }`; the pass path adds `bash "$LOG_EVENT" Stop verify pass ""` immediately before the final `exit 0`. Do NOT add a log call to the jq-absent early-exit branch (no jq available there — Pitfall 3; leave it exactly as-is). Keep `set -o pipefail`, the loop guard, the jq-absent branch, and the entire stack-detect block unchanged. Then extend `.claude/hooks/tests/stop-verify.test.sh`: KEEP the existing three checks verbatim and ADD a fourth — feed `{"stop_hook_active":true}` with a mktemp CLAUDE_PROJECT_DIR, assert exit 0 and that `tail -1 $dir/logs/$(date -u +%F).jsonl | jq -r '.decision'` == `loop-yield`; and a fifth asserting exit stays 0 when `logs` is an unwritable file. Run the test → GREEN.
  </action>
  <acceptance_criteria>
    - source: `grep -q "log-event.sh Stop verify loop-yield" .claude/hooks/stop-verify.sh`; pass and fail arms also call `log-event.sh Stop verify pass|fail` before their literal exits.
    - source (regression): `grep -q 'set -o pipefail' .claude/hooks/stop-verify.sh` still holds and precedes the stack-detect block; the loop-guard and jq-absent branches are unchanged; NO log call added to the jq-absent branch.
    - test-command (SC1/D-03): `{"stop_hook_active":true}` with temp CLAUDE_PROJECT_DIR → exit 0 AND last JSONL line `.decision`==loop-yield.
    - test-command (D-05): `logs` an unwritable file + `{"stop_hook_active":true}` → exit 0 unchanged.
    - test-command (Phase 1 regression): `bash .claude/hooks/tests/stop-verify.test.sh` exits 0 with all five checks passing; `bash -n .claude/hooks/stop-verify.sh` clean.
  </acceptance_criteria>
  <verify>
    <automated>bash -n .claude/hooks/stop-verify.sh && bash .claude/hooks/tests/stop-verify.test.sh</automated>
  </verify>
  <done>Stop fires log pass/fail/loop-yield without changing the gate's exit code (2/0) or the Phase 1 pipefail/loop-guard behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add session-handoff log calls (start/save) + new test (GREEN)</name>
  <files>.claude/hooks/session-handoff.sh, .claude/hooks/tests/session-handoff.test.sh</files>
  <read_first>
    - .claude/hooks/session-handoff.sh (the file being edited — arg-driven start/save, ignores stdin; keep the HANDOFF.md write logic)
    - .claude/hooks/tests/pretool-guard.test.sh (Phase 1 harness style to match)
    - .claude/hooks/log-event.sh (helper contract — Plan 01)
    - .planning/phases/02-observability-keystone-jsonl-event-log/02-RESEARCH.md §"Code Examples" (session-handoff wiring)
    - .planning/phases/02-observability-keystone-jsonl-event-log/02-CONTEXT.md §Decisions D-03, D-05
  </read_first>
  <behavior>
    - start logs one line: `session-handoff.sh start` with CLAUDE_PROJECT_DIR set → exit 0 AND the daily JSONL gains one line with `.event`==SessionStart, `.decision`==start.
    - save logs one line + writes handoff: `session-handoff.sh save` from a temp CWD → exit 0, writes `specs/HANDOFF.md`, AND the JSONL gains one line with `.event`==PreCompact, `.decision`==save.
    - exit code unchanged under log failure: with `logs` an unwritable file, both start and save still exit 0.
  </behavior>
  <action>
    Edit session-handoff.sh surgically. Add `LOG_EVENT="$(dirname "${BASH_SOURCE[0]}")/log-event.sh"` after the `H=` line. In the `start)` arm, after the existing restore (`[ -f "$H" ] && { ...; cat "$H"; }`), add `bash "$LOG_EVENT" SessionStart handoff start ""`. In the `save)` arm, after the existing `{ ...; } > "$H"` write, add `bash "$LOG_EVENT" PreCompact handoff save ""`. Keep the final `exit 0` and the HANDOFF.md content logic (including the current `[자동 수집 — 내용없음]` TODO placeholder — replacing that sentinel is STATE-01/Phase 3, explicitly out of Phase 2 scope; do not touch it). Then create `.claude/hooks/tests/session-handoff.test.sh` in the Phase 1 style: run `start` and `save` from a temp CWD (with `mkdir specs`) and a mktemp CLAUDE_PROJECT_DIR; assert exit 0, the correct `.event`/`.decision` on the last JSONL line for each, `specs/HANDOFF.md` written on save, and exit 0 preserved when `logs` is an unwritable file. `chmod +x` the test. Run it → GREEN.
  </action>
  <acceptance_criteria>
    - source: `grep -q "log-event.sh SessionStart handoff start" .claude/hooks/session-handoff.sh` AND `grep -q "log-event.sh PreCompact handoff save" .claude/hooks/session-handoff.sh`.
    - source: the final `exit 0` and the HANDOFF.md write logic are preserved; the `[자동 수집 — 내용없음]` placeholder is untouched (Phase 3 scope).
    - test-command (SC1/D-03): `start` → last JSONL line `.event`==SessionStart, `.decision`==start, exit 0; `save` → `.event`==PreCompact, `.decision`==save, exit 0, `specs/HANDOFF.md` exists.
    - test-command (D-05): `logs` an unwritable file → both start and save exit 0.
    - test-command (syntax): `bash -n .claude/hooks/session-handoff.sh` clean; `.claude/hooks/tests/session-handoff.test.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n .claude/hooks/session-handoff.sh && bash .claude/hooks/tests/session-handoff.test.sh</automated>
  </verify>
  <done>Session start/save each log exactly one line (SessionStart:start / PreCompact:save) without changing the hook's exit 0; HANDOFF.md write is preserved. All 5 hook entry points now log (D-03 complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Stop event → stop-verify.sh stdin | Untrusted `stop_hook_active`; the gate's block (exit 2) must be independent of logging (D-05). |
| session-handoff arg (start/save) → subprocess log | The handoff hook must stay exit 0 regardless of logging outcome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Elevation (bypass) | A log call flips stop-verify's block (exit 2) or loop-guard/pass (exit 0) | mitigate | Bare subprocess log call inserted BEFORE the existing literal `exit`; the exit is never replaced. Proven by: unwritable-logs → loop-yield still exit 0; source-assert log precedes literal exit. |
| T-02-12 | Elevation (bypass) | `set -o pipefail` / loop-guard regression while editing stop-verify | mitigate | Surgical insert only; Phase 1's five asserts (pipefail grep, loop marker, jq-absent) all still pass. |
| T-02-13 | Repudiation | Stop / session lifecycle events unrecorded, leaving an incomplete audit trail | mitigate | log-event.sh calls at loop-yield/pass/fail and start/save complete D-03's 5-hook coverage. Proven by: loop-yield and start/save produce one JSONL line each. |
| T-02-14 | Repudiation | jq-absent Stop yields no log line | accept | No jq → the jq-based logger cannot run; the gate's exit code is preserved (Pitfall 3). Documented gap. |
| T-02-SC | Tampering | Supply-chain (package installs) | accept — N/A | No packages installed; Bash + jq + coreutils only (SC3). |

Residual `high` with no mitigation: none.
</threat_model>

<verification>
- `bash -n` clean on stop-verify.sh and session-handoff.sh.
- `bash .claude/hooks/tests/stop-verify.test.sh` exits 0 (five checks incl. loop-yield logs one line).
- `bash .claude/hooks/tests/session-handoff.test.sh` exits 0 (start/save log + HANDOFF.md written).
- All 5 hook entry points now emit a log line (D-03 complete across Plans 01–03).
</verification>

<success_criteria>
- ROADMAP SC1 provable for Stop and session events: each fire appends exactly one JSON line carrying event/decision/timestamp; `jq .` parses.
- D-03 complete: pretool-guard (01) + posttool-format (02) + stop-verify + session-handoff (03) = all 5 hook entry points log.
- D-05 upheld: no log call changes stop-verify's 2/0 or session-handoff's 0.
- ROADMAP SC3 upheld: `bash -n` clean; no new tooling.
</success_criteria>

<output>
Create `.planning/phases/02-observability-keystone-jsonl-event-log/02-03-SUMMARY.md` when done.
</output>
