---
phase: 01-fail-closed-enforcement-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/common/git-workflow.md
  - .claude/rules/common/security.md
  - .claude/rules/common/testing.md
  - .claude/commands/commit.md
autonomous: true
requirements: [CFG-01, CFG-03]
must_haves:
  truths:
    - "None of the three common/ rules carry a paths: key — they are always-on (survive compaction re-injection) (D-04)"
    - "The java-spring and react-next stack rules remain extension-scoped (untouched) (D-05)"
    - "commit.md carries disable-model-invocation: true so it cannot be auto-invoked by the model"
  artifacts:
    - path: ".claude/rules/common/git-workflow.md"
      provides: "Always-on git rule (no frontmatter paths key)"
    - path: ".claude/rules/common/security.md"
      provides: "Always-on security rule (no frontmatter paths key)"
    - path: ".claude/rules/common/testing.md"
      provides: "Always-on testing rule (no frontmatter paths key)"
    - path: ".claude/commands/commit.md"
      provides: "Side-effect command guarded from model auto-invocation"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/commands/commit.md frontmatter"
      to: "model auto-invocation gate"
      via: "disable-model-invocation: true"
      pattern: "disable-model-invocation: true"
---

<objective>
Make the critical rules always-on and stop the side-effect `commit` command from being auto-invoked by the model. Remove the version-fragile `paths:` frontmatter from all three `common/` rules (so they are injected regardless of file and survive compaction re-injection), and add `disable-model-invocation: true` to `commit.md`.

Purpose: MANUAL §6 flags `rules/` `paths:` frontmatter as version-drift-risky (⚠️ 재확인) — the exact C10 drift this phase fights. D-04 immunizes the three critical `common/` rules by removing the key entirely (they are currently `paths: ["**/*"]`, so glob-loading is pure overhead and a drift surface). D-05 keeps the stack rules extension-scoped. CFG-03 prevents the model from auto-running a git side-effect command.

Output: Three rule files with their frontmatter removed and `commit.md` carrying the no-auto-invoke flag — all provable by grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fail-closed-enforcement-core/01-CONTEXT.md
@.planning/phases/01-fail-closed-enforcement-core/01-RESEARCH.md

<interfaces>
<!-- Ground-truth current frontmatter (verified by raw read during planning). -->
Each of the three common/ rules currently begins with EXACTLY:
  line 1: ---
  line 2: paths: ["**/*"]
  line 3: ---
  line 4+: # <heading> … (markdown body)
The ONLY frontmatter key is `paths:`. Removing the key ⇒ remove the whole 3-line block (leaving no empty `---/---`).

DO NOT TOUCH (D-05 — extension-scoped, must stay scoped):
  .claude/rules/java-spring/*  → paths: ["**/*.java"]
  .claude/rules/react-next/*   → paths: ["**/*.ts", "**/*.tsx"]

commit.md current frontmatter (CFG-03 target):
  ---
  description: 커밋 룰(commitlint) 준수 메시지로 커밋을 준비한다
  ---
`disable-model-invocation: true` is a documented command frontmatter field (RESEARCH §CFG Mechanics; docs cite /commit as the canonical use). Default is false.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove paths: frontmatter from the three common/ rules (CFG-01)</name>
  <files>.claude/rules/common/git-workflow.md, .claude/rules/common/security.md, .claude/rules/common/testing.md</files>
  <read_first>
    - .claude/rules/common/git-workflow.md
    - .claude/rules/common/security.md
    - .claude/rules/common/testing.md
    - .planning/phases/01-fail-closed-enforcement-core/01-CONTEXT.md §Decisions D-04, D-05
    - HARNESS-TEMPLATE-MANUAL.md §6 (paths: frontmatter version-drift rationale)
  </read_first>
  <action>
    For EACH of the three files, delete the leading YAML frontmatter block — the three lines `---`, `paths: ["**/*"]`, `---` — so the file now starts directly at its `#` markdown heading. Since `paths:` is the only key, removing the key per D-04 means removing the entire block (do not leave an empty `---`/`---`). Edit the markdown body of NONE of them (surgical — the rule text is not in scope). Do NOT touch `java-spring/` or `react-next/` rules (D-05 — they stay extension-scoped).
  </action>
  <acceptance_criteria>
    - source: for each of the three files, `grep -c '^paths:' <file>` returns 0 (key gone).
    - source: for each, the first line is now the markdown heading, not `---` (`head -1 <file>` starts with `#`).
    - source (D-05 untouched): `grep -q '^paths:' .claude/rules/java-spring/*.md` AND `grep -q '^paths:' .claude/rules/react-next/*.md` still hold (stack rules unchanged).
    - test-command (SC#5): for f in git-workflow security testing; do `grep -q '^paths:' ".claude/rules/common/$f.md" && echo "FAIL $f" || echo "OK $f"`; done → three OK, zero FAIL.
    - The markdown body text of each rule is byte-identical to before except for the removed frontmatter block.
  </acceptance_criteria>
  <verify>
    <automated>for f in git-workflow security testing; do grep -q '^paths:' ".claude/rules/common/$f.md" && exit 1; done; grep -q '^paths:' .claude/rules/java-spring/*.md && grep -q '^paths:' .claude/rules/react-next/*.md</automated>
  </verify>
  <done>All three common/ rules are always-on (no paths: key); stack rules remain extension-scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Add disable-model-invocation to commit.md (CFG-03)</name>
  <files>.claude/commands/commit.md</files>
  <read_first>
    - .claude/commands/commit.md
    - .planning/phases/01-fail-closed-enforcement-core/01-RESEARCH.md §"State of the Art" (commit.md keeps working with the added frontmatter key)
  </read_first>
  <action>
    Add the frontmatter key `disable-model-invocation: true` to the existing YAML frontmatter block of `commit.md`, alongside the existing `description:` key (do not remove or reword `description`). The body text stays unchanged. This blocks the model from auto-invoking the `/commit` side-effect command (it remains user-invocable).
  </action>
  <acceptance_criteria>
    - source (SC#5): `grep -q 'disable-model-invocation: true' .claude/commands/commit.md`.
    - source: the existing `description:` key is still present and unchanged.
    - source: the frontmatter is still a single valid `---`-delimited block (exactly two `---` delimiter lines at the top).
    - The command body text is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'disable-model-invocation: true' .claude/commands/commit.md && grep -q '^description:' .claude/commands/commit.md</automated>
  </verify>
  <done>commit.md carries disable-model-invocation: true and cannot be auto-invoked by the model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code version upgrade → rule loading | `paths:` glob loading is version-fragile (MANUAL §6); a drift can silently drop a critical rule from context. |
| Model reasoning → side-effect command | The model auto-invoking `/commit` performs a git side-effect without explicit user intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation (constraint bypass) | Critical rule silently dropped after compaction / version drift due to `paths:` glob | mitigate | Remove `paths:` key from all three common/ rules → always-on injection (D-04). Proven by grep == 0. |
| T-03-02 | Tampering (unintended side-effect) | Model auto-invokes `/commit` (git side-effect) without user intent | mitigate | `disable-model-invocation: true` on commit.md (CFG-03). Proven by grep. |
| T-03-03 | Elevation | Over-broad change promoting stack rules to always-on (scope creep) | accept/avoid | D-05: java/react rules stay extension-scoped; acceptance_criteria asserts their `paths:` survives. |
| T-03-SC | Tampering | Supply-chain (package installs) | accept — N/A | No packages installed; frontmatter edits only. |

Residual `high` with no mitigation: none.
</threat_model>

<verification>
- Three `common/` rules: `grep -c '^paths:'` == 0 each; first line is the `#` heading.
- Stack rules (`java-spring`, `react-next`): `paths:` still present (untouched).
- `grep -q 'disable-model-invocation: true' .claude/commands/commit.md` holds; `description:` intact.
</verification>

<success_criteria>
- ROADMAP SC#5 (commit has disable-model-invocation; critical rules have no paths:) partially provable here (the $schema half is Plan 04): grep asserts commit flag + zero paths: on the three rules.
- CFG-01, CFG-03 satisfied by grep assertions.
</success_criteria>

<output>
Create `.planning/phases/01-fail-closed-enforcement-core/01-03-SUMMARY.md` when done.
</output>
