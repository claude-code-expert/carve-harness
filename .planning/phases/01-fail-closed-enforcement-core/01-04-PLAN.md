---
phase: 01-fail-closed-enforcement-core
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - .claude/settings.json
  - .claude/hooks/tests/settings.test.sh
autonomous: true
requirements: [GUARD-02, GATE-02, CFG-02, CFG-04]
must_haves:
  truths:
    - "The PreToolUse matcher is Write|Edit|MultiEdit|NotebookEdit|Bash so the guard fires on every write tool plus Bash"
    - "All five hook command strings resolve via ${CLAUDE_PROJECT_DIR} so the guard still fires from a subdirectory"
    - "The Stop hook carries an explicit timeout (900s) that clearly exceeds the current 600s default and real builds (D-06)"
    - "settings.json declares a top-level $schema and still parses as valid JSON"
  artifacts:
    - path: ".claude/settings.json"
      provides: "Expanded matcher, project-root hook resolution, explicit Stop timeout, schema declaration"
      contains: "$schema"
    - path: ".claude/hooks/tests/settings.test.sh"
      provides: "jq/grep assertions for matcher, ${CLAUDE_PROJECT_DIR}, timeout, $schema, and subdirectory resolution"
      contains: "CLAUDE_PROJECT_DIR"
  key_links:
    - from: ".claude/settings.json PreToolUse matcher"
      to: ".claude/hooks/pretool-guard.sh"
      via: "matcher Write|Edit|MultiEdit|NotebookEdit|Bash fires the guard"
      pattern: "MultiEdit|NotebookEdit|Bash"
    - from: ".claude/settings.json hook commands"
      to: "${CLAUDE_PROJECT_DIR}"
      via: "project-root resolution"
      pattern: "CLAUDE_PROJECT_DIR"
---

<objective>
Wire the hardened hooks so they actually fire and cannot be silently disabled. Expand the PreToolUse matcher to cover every write tool plus Bash (GUARD-02/03 wiring), prefix all five hook commands with `${CLAUDE_PROJECT_DIR}` so they resolve from the project root regardless of cwd (CFG-02), add an explicit generous Stop `timeout` (GATE-02), and declare a `$schema` for early version-drift detection (CFG-04).

Purpose: Plan 01 taught `pretool-guard.sh` to handle NotebookEdit and Bash, but the live matcher is still `Write|Edit` — so those tools never reach the guard. This plan closes the wiring gap. `${CLAUDE_PROJECT_DIR}` fixes SC#4's "fires from a subdirectory." The explicit Stop `timeout` eliminates silent truncation — and per RESEARCH Pitfall 4 the default is now 600s (not 60s), so the value must be visibly higher to read as intent.

Depends on Plan 01 (guard must handle the new tools before the matcher routes them) and Plan 02 (the Stop-gate script hardening pairs with the Stop timeout). Runtime note: settings.json is read at session start, so live in-session triggering cannot be tested — all assertions are jq/grep on the file plus a `${CLAUDE_PROJECT_DIR}` subdirectory-resolution invocation of the guard script.

Output: An edited `settings.json` and a test proving matcher coverage, project-root resolution, timeout, and schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fail-closed-enforcement-core/01-CONTEXT.md
@.planning/phases/01-fail-closed-enforcement-core/01-RESEARCH.md
@.planning/phases/01-fail-closed-enforcement-core/01-01-SUMMARY.md

<interfaces>
<!-- Current settings.json shape (verified by raw read during planning). Five hook command strings total. -->
  hooks.PreToolUse[0].matcher   : "Write|Edit"           → widen to "Write|Edit|MultiEdit|NotebookEdit|Bash"
  hooks.PreToolUse[0].hooks[0].command  : "bash .claude/hooks/pretool-guard.sh"
  hooks.PostToolUse[0].matcher  : "Write|Edit"           → LEAVE AS-IS (formatting scope, not a Phase-1 guard concern)
  hooks.PostToolUse[0].hooks[0].command : "bash .claude/hooks/posttool-format.sh"
  hooks.Stop[0].hooks[0].command        : "bash .claude/hooks/stop-verify.sh"     → add "timeout": 900
  hooks.SessionStart[0].hooks[0].command: "bash .claude/hooks/session-handoff.sh start"
  hooks.PreCompact[0].hooks[0].command  : "bash .claude/hooks/session-handoff.sh save"

<!-- CFG-02: prefix EVERY command's script path with ${CLAUDE_PROJECT_DIR}, e.g.
     "bash ${CLAUDE_PROJECT_DIR}/.claude/hooks/pretool-guard.sh" (5 commands total). -->
<!-- GATE-02: default command-hook timeout is now 600s (RESEARCH Pitfall 4). 900 chosen to be
     unambiguously above the default AND above real gradle+vitest builds. timeout unit = seconds. -->
<!-- CFG-04: top-level "$schema": "https://json.schemastore.org/claude-code-settings.json" (advisory/editor-side). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing jq/grep assertions for settings.json (RED)</name>
  <files>.claude/hooks/tests/settings.test.sh</files>
  <read_first>
    - .claude/settings.json (current shape — the RED baseline: matcher is Write|Edit, no timeout, no $schema, no ${CLAUDE_PROJECT_DIR})
    - .planning/phases/01-fail-closed-enforcement-core/01-RESEARCH.md §"Code Examples" (CFG verification asserts), §"Pitfall 4", §"Runtime State Inventory"
  </read_first>
  <behavior>
    - `jq -e '."$schema"' .claude/settings.json` → present (CFG-04)
    - `.hooks.PreToolUse[0].matcher` == `Write|Edit|MultiEdit|NotebookEdit|Bash` (GUARD-02)
    - `.hooks.Stop[0].hooks[0].timeout` present AND `> 600` (GATE-02)
    - all five hook command strings contain `${CLAUDE_PROJECT_DIR}` → `grep -o 'CLAUDE_PROJECT_DIR' .claude/settings.json | wc -l` == 5 (CFG-02)
    - `jq . .claude/settings.json` still parses (validity)
    - subdirectory resolution (SC#4): from a non-root cwd, `CLAUDE_PROJECT_DIR=<repo-root> bash -c 'cd "$(mktemp -d)" && printf "%s" "{\"tool_name\":\"Write\",\"tool_input\":{\"file_path\":\"x/.env.production\"}}" | bash ${CLAUDE_PROJECT_DIR}/.claude/hooks/pretool-guard.sh"' exits 2
  </behavior>
  <action>
    Create the harness under `.claude/hooks/tests/`. Resolve the repo root once (e.g. `root=$(cd "$(dirname "$0")/../../.." && pwd)`), then assert each of the six behaviors above with jq/grep/exit-code checks, printing PASS/FAIL and exiting non-zero on any failure. For the subdirectory check, set `CLAUDE_PROJECT_DIR="$root"` and invoke the guard from a fresh temp dir via the `${CLAUDE_PROJECT_DIR}`-prefixed path, asserting exit 2 (this depends on Plan 01's guard already blocking `.env.production`). Shebang + `chmod +x`. End RED: run against the current settings.json → the $schema, matcher, timeout, and ${CLAUDE_PROJECT_DIR} assertions all fail.
  </action>
  <acceptance_criteria>
    - source: `.claude/hooks/tests/settings.test.sh` exists, executable, shebang present.
    - behavior: asserts all six behaviors in `<behavior>`.
    - test-command (RED proof): `bash .claude/hooks/tests/settings.test.sh; test $? -ne 0`.
  </acceptance_criteria>
  <verify>
    <automated>bash .claude/hooks/tests/settings.test.sh; test $? -ne 0 && echo "RED confirmed"</automated>
  </verify>
  <done>Test harness exists and fails against the current settings.json, making every wiring gap observable.</done>
</task>

<task type="auto">
  <name>Task 2: Apply matcher, ${CLAUDE_PROJECT_DIR}, timeout, and $schema edits (GREEN)</name>
  <files>.claude/settings.json</files>
  <read_first>
    - .claude/settings.json (the file being edited)
    - .planning/phases/01-fail-closed-enforcement-core/01-RESEARCH.md §"Pattern 4" (${CLAUDE_PROJECT_DIR}), §"Pitfall 4" (timeout default 600s), §"CFG Mechanics"/§"State of the Art" ($schema, disable-model-invocation)
    - .planning/phases/01-fail-closed-enforcement-core/01-CONTEXT.md §Decisions D-06 (explicit generous timeout)
  </read_first>
  <action>
    Make four edits, keeping the JSON valid throughout: (1) GUARD-02 — set `hooks.PreToolUse[0].matcher` to the exact string `Write|Edit|MultiEdit|NotebookEdit|Bash`; leave `hooks.PostToolUse[0].matcher` as `Write|Edit` (formatting scope, out of Phase 1 guard concern — surgical). (2) CFG-02 — prefix the script path in ALL FIVE hook command strings (PreToolUse, PostToolUse, Stop, SessionStart, PreCompact) with `${CLAUDE_PROJECT_DIR}`, e.g. `bash ${CLAUDE_PROJECT_DIR}/.claude/hooks/pretool-guard.sh` and `bash ${CLAUDE_PROJECT_DIR}/.claude/hooks/session-handoff.sh save` (preserve each command's existing argument). (3) GATE-02 — add `"timeout": 900` to the Stop hook command object (`hooks.Stop[0].hooks[0]`), seconds; 900 is chosen deliberately above the current 600s default (RESEARCH Pitfall 4) and above real gradle+vitest builds. (4) CFG-04 — add a top-level key `"$schema": "https://json.schemastore.org/claude-code-settings.json"`. Do not reorder or drop the existing `permissions` block. Run Task 1's harness → GREEN.
  </action>
  <acceptance_criteria>
    - source (validity): `jq . .claude/settings.json` parses without error.
    - source (GUARD-02): `jq -r '.hooks.PreToolUse[0].matcher' .claude/settings.json` == `Write|Edit|MultiEdit|NotebookEdit|Bash`.
    - source (PostToolUse untouched): `jq -r '.hooks.PostToolUse[0].matcher' .claude/settings.json` == `Write|Edit`.
    - source (CFG-02): `grep -o 'CLAUDE_PROJECT_DIR' .claude/settings.json | wc -l` == 5.
    - source (GATE-02): `jq -e '.hooks.Stop[0].hooks[0].timeout > 600' .claude/settings.json` is true.
    - source (CFG-04): `jq -e '."$schema"' .claude/settings.json` is non-null.
    - source (permissions preserved): `jq -e '.permissions.deny | length > 0' .claude/settings.json` is true.
    - test-command (SC#4 subdirectory resolution): `root=$(pwd); CLAUDE_PROJECT_DIR="$root" bash -c 'cd "$(mktemp -d)" && printf "%s" "{\"tool_name\":\"Write\",\"tool_input\":{\"file_path\":\"x/.env.production\"}}" | bash ${CLAUDE_PROJECT_DIR}/.claude/hooks/pretool-guard.sh"; test $? -eq 2`.
    - test-command (GREEN): `bash .claude/hooks/tests/settings.test.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>jq . .claude/settings.json >/dev/null && bash .claude/hooks/tests/settings.test.sh</automated>
  </verify>
  <done>The matcher covers all write tools plus Bash, every hook resolves from the project root, the Stop gate has an explicit generous timeout, and settings.json declares a $schema and still parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code version upgrade → settings.json | Silent default drift (e.g. timeout 60s→600s) can disable a gate; matcher gaps let write-capable tools skip the guard. |
| Subdirectory / monorepo cwd → hook resolution | A cwd-relative hook path fails to resolve from a subdirectory, silently disabling the guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Matcher gap — MultiEdit/NotebookEdit/Bash not routed to the guard | mitigate | Expand PreToolUse matcher to `Write|Edit|MultiEdit|NotebookEdit|Bash` (GUARD-02). Proven by jq equality assert. |
| T-04-02 | Elevation (bypass) | Guard silently disabled when Claude runs from a subdirectory (cwd-relative path) | mitigate | Prefix all five commands with `${CLAUDE_PROJECT_DIR}` (CFG-02). Proven by subdirectory-invocation exit-2 assert. |
| T-04-03 | Denial of Service | Stop gate silently truncated by default timeout (a cancelled command hook is non-blocking → gate bypassed) | mitigate | Explicit `timeout: 900` > current 600s default and > real builds (GATE-02, D-06, Pitfall 4). Proven by `> 600` assert. |
| T-04-04 | Tampering (version drift) | settings.json shape drifts across versions unnoticed | mitigate | Declare `$schema` for editor-side early drift detection (CFG-04). Advisory-only; residual is low. |
| T-04-05 | Tampering | Broken JSON disabling ALL hooks at once | mitigate | `jq .` validity gate in both tasks; permissions block preserved. |
| T-04-SC | Tampering | Supply-chain (package installs) | accept — N/A | No packages installed; the `$schema` URL is an editor hint, not an installed dependency. |

Residual `high` with no mitigation: none. (T-04-04 residual is low — `$schema` is advisory per RESEARCH A1.)
</threat_model>

<verification>
- `jq . .claude/settings.json` parses.
- Matcher == `Write|Edit|MultiEdit|NotebookEdit|Bash`; PostToolUse still `Write|Edit`.
- `grep -o CLAUDE_PROJECT_DIR | wc -l` == 5; Stop `timeout > 600`; `$schema` present; permissions preserved.
- Subdirectory `${CLAUDE_PROJECT_DIR}` invocation of the guard on `.env.production` → exit 2.
- `bash .claude/hooks/tests/settings.test.sh` exits 0.
</verification>

<success_criteria>
- ROADMAP SC#4 (subdirectory resolution) provable: `${CLAUDE_PROJECT_DIR}` guard invocation from a temp dir → exit 2.
- ROADMAP SC#5 ($schema validates + still parses) provable: `jq -e '."$schema"'` non-null and `jq .` clean.
- GUARD-02 (matcher wiring), GATE-02, CFG-02, CFG-04 satisfied by jq/grep assertions.
</success_criteria>

<output>
Create `.planning/phases/01-fail-closed-enforcement-core/01-04-SUMMARY.md` when done.
</output>
