---
phase: 01-m8-lifecycle-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/installer.ts
  - test/e2e/installer.e2e.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-05]
must_haves:
  truths:
    - "A fresh install writes a schema-v2 manifest where every installed file records {path, hash, assetVersion} and schemaVersion is present"
    - "hash is computed at write time by the installer (node:crypto) — generator/Artifact stay pure value objects"
    - "An audit failure path never reaches manifest write (manifest is written LAST, after all artifact writes succeed) so a prior install/manifest is unchanged on failure"
    - "uninstall iterates m.files as objects and remains non-destructive for both v1 (normalized) and v2 manifests"
  artifacts:
    - path: "src/installer.ts"
      provides: "writeArtifacts returns ManifestFile[]; install/installClaudeBase write schemaVersion + ManifestFile[]; unionFiles dedup by path; uninstall destructures {path}"
      contains: "schemaVersion"
    - path: "test/e2e/installer.e2e.test.ts"
      provides: "Assertions that manifest is v2 with non-empty hashes; existing string[] references updated to f.path"
      contains: "schemaVersion"
  key_links:
    - from: "src/installer.ts writeArtifacts"
      to: "manifest hashContent"
      via: "hash computed per artifact at write time"
      pattern: "hashContent"
    - from: "src/installer.ts install"
      to: "writeManifest"
      via: "manifest written LAST after artifacts"
      pattern: "writeManifest"
---

<objective>
Move the installer onto manifest schema v2: compute each file's content hash at write time (installer owns hashing, NOT the generator — `Artifact` stays a pure `{path,content,executable}` value object), record `{path, hash, assetVersion}` per file, stamp `schemaVersion`, and dedup the file union by path. Preserve the existing `.bak`-once rule, settings merge, and non-destructive uninstall. Reinforce manifest-last atomicity (manifest write is already last; keep it that way and verify nothing writes the manifest before all artifacts land).

Purpose: Make every install produce the v2 record that `diff`/`update` (01-03) classify against, without touching the generator or its tests.
Output: Updated `src/installer.ts` + augmented `test/e2e/installer.e2e.test.ts`.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.claude/rules/code-style.md
@.claude/rules/safety.md
@.planning/phases/01-m8-lifecycle-foundation/01-01-SUMMARY.md

<interfaces>
From src/manifest.ts (v2 — delivered by 01-01):
```typescript
export const SCHEMA_VERSION = 2;
export interface ManifestFile { path: string; hash: string; assetVersion: string }
export interface Manifest {
  schemaVersion: number;
  version: string;
  files: ManifestFile[];
  backups: string[];
  hooks: ManifestHook[];
  mcps?: string[];
}
export function hashContent(content: string): string; // sha256 hex
export function readManifest(root: string): Manifest | null; // returns normalized v2
export const CARVE_VERSION: string;
```

From src/generator.ts (UNCHANGED — do not modify):
```typescript
export interface Artifact { path: string; content: string; executable: boolean }
```

Current src/installer.ts call sites that MUST change (files element becomes object):
- writeArtifacts returns string[] -> must return ManifestFile[]
- install: `prevFiles = new Set(prev?.files ?? [])` -> Set of paths from prev.files.map(f=>f.path)
- install/installClaudeBase: manifest `files: written` (string[]) -> ManifestFile[] + add schemaVersion
- installClaudeBase: `[...new Set([...(prev?.files ?? []), ...written])]` -> union dedup BY PATH
- uninstall: `for (const f of m.files)` where f was string -> `for (const { path } of m.files)`
- InstallResult.written stays string[] for log compatibility -> map ManifestFile to .path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: installer writes ManifestFile[] with hashes (schema v2)</name>
  <files>src/installer.ts</files>
  <read_first>
    - src/installer.ts (the file being modified — full current implementation)
    - src/manifest.ts (v2 contract from 01-01: ManifestFile, hashContent, SCHEMA_VERSION)
    - src/generator.ts lines 1-20 (Artifact type — MUST stay unchanged)
    - .claude/rules/safety.md (idempotency + .bak-once contract — must not weaken)
  </read_first>
  <behavior>
    - After a fresh install, readManifest(root).schemaVersion === 2.
    - Every entry in manifest.files has a non-empty hash equal to hashContent(artifact.content) and assetVersion === CARVE_VERSION.
    - Re-install (idempotent) overwrites each file's hash for the same path; no duplicate path entries appear in manifest.files (union dedup by path).
    - .bak-once rule unchanged: a pre-existing non-carve file is backed up exactly once; re-install adds no new .bak.
    - InstallResult.written remains string[] (paths) so existing log/test call sites still read r.written.length.
  </behavior>
  <action>
    Update src/installer.ts to schema v2. Import `hashContent`, `SCHEMA_VERSION`, and `type ManifestFile` from './manifest.ts'.
    Change `writeArtifacts` to return `ManifestFile[]`: for each artifact, after writing, push `{ path: a.path, hash: hashContent(a.content), assetVersion: CARVE_VERSION }`. The `.bak`-once logic and prevFiles path-membership check stay identical (prevFiles is a Set<string> of paths).
    In `install`: build `prevFiles` from `new Set((prev?.files ?? []).map((f) => f.path))`. Build the manifest with `schemaVersion: SCHEMA_VERSION`, `files: written` (now ManifestFile[]). Set `InstallResult.written` to `written.map((f) => f.path)` so the return shape and the log line (`r.written.length`) are unchanged.
    In `installClaudeBase`: same prevFiles-by-path change. The ROOT_CLAUDE append branch must push a ManifestFile for `CLAUDE.md` (hash of the final root content written, assetVersion CARVE_VERSION) when it is not already in `written`. Replace the `[...new Set([...prev.files, ...written])]` union with a `unionFiles(prev?.files ?? [], written)` helper that dedups BY PATH (later entry wins, so a re-install refreshes the hash). Add `schemaVersion: SCHEMA_VERSION` to the written manifest. Keep the InstallResult.written = paths mapping.
    Add a small private helper `function unionFiles(a: ManifestFile[], b: ManifestFile[]): ManifestFile[]` keyed by path (Map<path, ManifestFile>, b overrides a).
    Confirm `writeManifest(...)` remains the LAST write in both install and installClaudeBase (manifest-last atomicity — D8/C7). Do NOT introduce any manifest write before artifacts/settings.
    Do NOT modify src/generator.ts. Korean comments consistent with file style.
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - src/installer.ts contains `schemaVersion: SCHEMA_VERSION` in both install and installClaudeBase manifests.
    - writeArtifacts return type is `ManifestFile[]`; `hashContent(` appears in writeArtifacts.
    - `unionFiles` helper dedups by path; installClaudeBase uses it.
    - `npm run check` exits 0 (manifest type errors from 01-01 at installer call sites are now resolved).
    - src/generator.ts is unmodified (git diff shows no generator changes).
  </acceptance_criteria>
  <done>Installer computes hashes at write, records ManifestFile[] + schemaVersion, dedups by path, generator untouched, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: uninstall over objects + e2e v2 assertions</name>
  <files>src/installer.ts, test/e2e/installer.e2e.test.ts</files>
  <read_first>
    - src/installer.ts (as modified in Task 1 — the uninstall function)
    - test/e2e/installer.e2e.test.ts (the file being modified — existing assertions reference m.files as string[])
    - src/manifest.ts (readManifest returns normalized v2)
  </read_first>
  <behavior>
    - uninstall iterates manifest files via `{ path }` destructure and removes/restores exactly as before — non-destructive, restores .bak, deletes manifest. Works identically whether the on-disk manifest was v1 (normalized to objects) or v2.
    - Existing e2e tests pass with element-shape updated: `m.files.length` still works; any `m.files` membership/value assertions now use `f.path`.
    - New assertions: after fresh install m.schemaVersion === 2 and m.files[0].hash !== '' and m.files[0].assetVersion is the carve version.
  </behavior>
  <action>
    Update `uninstall` in src/installer.ts to iterate `for (const { path } of m.files)` (rename the loop variable from the old string `f` to a `path` binding; keep removed/restored arrays pushing the path string). Behavior otherwise unchanged (rmSync, .bak copy-back, stripCarveHooks, stripCarveMcp, removeManifest). This must remain correct for normalized-v1 manifests too (readManifest already normalizes).
    Update test/e2e/installer.e2e.test.ts: keep ALL existing tests/assertions but fix references that treated `m.files` as `string[]` — e.g. assertions checking file membership now read `m.files.map(f => f.path)` or destructure `f.path`. The existing `m.files.length === 2` style stays valid. Add to the 'fresh install' test: `assert.equal(m.schemaVersion, 2)`, `assert.ok(m.files[0].hash !== '')`, and assert assetVersion equals the manifest version. Keep using the withTemp/mkdtemp pattern already in the file.
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness && node --test test/e2e/installer.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/installer.ts uninstall uses `for (const { path } of m.files)`.
    - test/e2e/installer.e2e.test.ts contains `schemaVersion` and a `hash !== ''` assertion.
    - `node --test test/e2e/installer.e2e.test.ts` exits 0 (all existing + new assertions pass).
  </acceptance_criteria>
  <done>uninstall consumes v2 objects non-destructively; e2e installer suite asserts schema v2 + non-empty hashes and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| carve process → target .claude/ + root guides | carve writes files into a partially user-owned tree; must not clobber user edits or leave inconsistent manifest |
| existing on-disk files → install overwrite | pre-existing user files must be .bak-preserved once before overwrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | install/installClaudeBase write order | mitigate | Manifest written LAST (after all artifact + settings writes); no inconsistent manifest if a write fails mid-way (C7/D8 manifest-last atomicity) |
| T-01-04 | Denial of Service | uninstall over user-owned tree | mitigate | uninstall only touches manifest-listed paths; restores .bak; never rm -rf a directory (D5/safety.md) |
| T-01-05 | Tampering | re-install duplicate file entries | mitigate | unionFiles dedups by path so manifest cannot accumulate stale/duplicate entries that would mis-drive uninstall |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (node:crypto stdlib). No legitimacy gate needed. |
</threat_model>

<verification>
- `npm run check` exits 0.
- `node --test test/e2e/installer.e2e.test.ts` exits 0.
- `git diff --stat src/generator.ts` shows no changes (generator stays pure).
</verification>

<success_criteria>
- Fresh install yields schema-v2 manifest with per-file hash + assetVersion.
- Re-install dedups by path and refreshes hashes; .bak-once preserved.
- uninstall is non-destructive over v2 objects (and normalized v1).
</success_criteria>

<output>
Create `.planning/phases/01-m8-lifecycle-foundation/01-02-SUMMARY.md` when done.
</output>
