---
phase: 01-m8-lifecycle-foundation
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/cli.ts
  - test/unit/cli.test.ts
  - test/e2e/lifecycle.e2e.test.ts
autonomous: true
requirements: [LIFE-02, LIFE-03, LIFE-04, LIFE-05, LIFE-06]
must_haves:
  truths:
    - "carve diff / carve update / carve migrate dispatch to their handlers; update passes {yes: --yes, force: --force}"
    - "USAGE lists the three new commands in Korean and KNOWN_COMMANDS includes update/diff/migrate"
    - "The full lifecycle roundtrip install->diff->update->migrate->uninstall passes with a user-modified asset surviving update"
    - "An induced audit failure during a lifecycle write leaves the prior install + manifest unchanged"
    - "A v1 manifest uninstall is non-destructive"
  artifacts:
    - path: "src/cli.ts"
      provides: "update/diff/migrate dispatch + USAGE + KNOWN_COMMANDS"
      contains: "cmdUpdate"
    - path: "test/e2e/lifecycle.e2e.test.ts"
      provides: "install->diff->update->migrate->uninstall roundtrip + atomicity + v1 non-destructive uninstall"
      contains: "lifecycle"
    - path: "test/unit/cli.test.ts"
      provides: "dispatch + USAGE assertions for the three new commands"
      contains: "diff"
  key_links:
    - from: "src/cli.ts run()"
      to: "cmdUpdate/cmdDiff/cmdMigrate"
      via: "switch cases dispatch"
      pattern: "cmdUpdate|cmdDiff|cmdMigrate"
---

<objective>
Wire the three new lifecycle commands into the CLI and prove the whole phase with an end-to-end roundtrip. `cli.ts` gains `update`/`diff`/`migrate` dispatch (update forwards `{yes, force}`), three Korean USAGE lines, and KNOWN_COMMANDS entries. The phase-closing e2e test exercises install→diff→update→migrate→uninstall with a user-modified asset preserved, an induced audit-failure atomicity check, and a v1-manifest non-destructive uninstall.

Purpose: Deliver LIFE-06 (roundtrip verified) and surface every M8 command to users; lock the ≥80 coverage gate at phase close.
Output: Updated `src/cli.ts`, `test/unit/cli.test.ts`, new `test/e2e/lifecycle.e2e.test.ts`.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.claude/rules/code-style.md
@.planning/phases/01-m8-lifecycle-foundation/01-04-SUMMARY.md

<interfaces>
From src/commands.ts (delivered by 01-03/01-04):
```typescript
export function cmdDiff(root: string, io: IO): number;
export function cmdUpdate(root: string, io: IO, opts: { yes?: boolean; force?: boolean }): number;
export function cmdMigrate(root: string, io: IO): number;
```

From src/cli.ts (CURRENT — extend, do not rewrite):
```typescript
export const KNOWN_COMMANDS = ['install','init-claude','uninstall','list','doctor'] as const; // add update/diff/migrate
export const USAGE = `...`; // add 3 Korean lines
export function run(args: string[], io: IO = console): number; // switch (cmd) — add cases
// existing import line: import { cmdList, cmdInstall, cmdDoctor, cmdUninstall, cmdInitClaude } from './commands.ts';
// dir resolution: const positionals = args.slice(1).filter(a=>!a.startsWith('-')); const dir = positionals[0] ?? process.cwd();
```

From src/installer.ts / src/manifest.ts (for the e2e test):
```typescript
export function install(root, artifacts, hooks?, mcps?): InstallResult;
export function uninstall(root): UninstallResult;
export function readManifest(root): Manifest | null;
export function writeManifest(root, m): void; // to plant a v1-shaped fixture, write raw JSON via fs instead
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cli.ts dispatch + USAGE + KNOWN_COMMANDS, cli unit test</name>
  <files>src/cli.ts, test/unit/cli.test.ts</files>
  <read_first>
    - src/cli.ts (the file being modified — full current run()/USAGE/KNOWN_COMMANDS)
    - test/unit/cli.test.ts (the file being modified — existing dispatch + USAGE assertions)
    - src/commands.ts (cmdDiff/cmdUpdate/cmdMigrate signatures)
  </read_first>
  <behavior>
    - `carve diff [dir]` dispatches to cmdDiff(dir, io); `carve migrate [dir]` to cmdMigrate(dir, io); `carve update [dir]` to cmdUpdate(dir, io, { yes: args.includes('--yes'), force: args.includes('--force') }).
    - KNOWN_COMMANDS includes 'update', 'diff', 'migrate'.
    - USAGE contains three new Korean lines describing diff/update/migrate.
    - An unknown command still returns 1 with USAGE.
  </behavior>
  <action>
    Update src/cli.ts: add `cmdUpdate, cmdDiff, cmdMigrate` to the import from './commands.ts'. Add `'update', 'diff', 'migrate'` to KNOWN_COMMANDS. Add three Korean lines to USAGE under the command list (e.g. `carve diff` — 설치 자산을 매니페스트/현재 carve 자산과 3-way 비교, `carve update` — carve 갱신분만 제자리 갱신·사용자 수정 보존(--force·--yes), `carve migrate` — v1 매니페스트를 v2로 무손실 이관). Add switch cases in run(): `case 'diff': return cmdDiff(dir, io);`, `case 'migrate': return cmdMigrate(dir, io);`, `case 'update': return cmdUpdate(dir, io, { yes: args.includes('--yes'), force: args.includes('--force') });`. Reuse the existing `dir` resolution.
    Update test/unit/cli.test.ts: keep existing tests; add assertions that `run(['diff', root], io)`, `run(['migrate', root], io)`, `run(['update', root], io)` return 0 against an installed temp root (mirror the existing doctor/install temp-root pattern in the file), and that USAGE contains the three new command tokens (`diff`, `update`, `migrate`). Add a KNOWN_COMMANDS membership assertion if the file already asserts on it.
    No `any`, no `!`.
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness && npm run check && node --test test/unit/cli.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/cli.ts KNOWN_COMMANDS includes 'update','diff','migrate'; run() has cases for all three; update case passes `{ yes, force }`.
    - USAGE contains 'diff', 'update', 'migrate' tokens.
    - test/unit/cli.test.ts asserts the three dispatches return 0 and USAGE includes the new commands.
    - `npm run check` exits 0; `node --test test/unit/cli.test.ts` exits 0.
  </acceptance_criteria>
  <done>Three lifecycle commands dispatch correctly with flag forwarding, USAGE/KNOWN_COMMANDS updated, cli unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lifecycle e2e roundtrip + atomicity + v1 non-destructive uninstall</name>
  <files>test/e2e/lifecycle.e2e.test.ts</files>
  <read_first>
    - test/e2e/installer.e2e.test.ts (withTemp/mkdtemp pattern, install/uninstall usage)
    - src/installer.ts (install/uninstall, .bak-once)
    - src/commands.ts (cmdDiff/cmdUpdate/cmdMigrate, classify)
    - src/manifest.ts (readManifest, writeManifest, migrateManifest)
  </read_first>
  <behavior>
    - Roundtrip: a fresh install (schema v2) -> diff reports classifications -> a user edits one installed asset -> update with --force preserves the user content as .bak once and refreshes carve-updated assets -> a user edit WITHOUT --force survives update untouched -> migrate on an already-v2 manifest is a no-op -> uninstall cleanly removes carve files and restores .bak.
    - Atomicity: an induced auditor failure during an update write leaves the prior install files AND the manifest byte-identical (nothing written).
    - v1 non-destructive uninstall: plant a v1-shaped manifest (files: string[]) on disk for installed files, then uninstall — all listed files are removed, no throw, manifest deleted.
  </behavior>
  <action>
    Create test/e2e/lifecycle.e2e.test.ts (node:test) using the withTemp/mkdtempSync helper pattern from installer.e2e.test.ts.
    Test A (roundtrip + user-edit preservation): install a small artifact set via install(); capture initial manifest. Simulate a carve-updated asset by installing again with changed content for one path and asserting classify/cmdUpdate refreshes it (or directly assert via classify from commands.ts + cmdUpdate against a capture IO). Edit an installed file on disk to simulate user-modified; run cmdUpdate WITHOUT force and assert the user content is intact afterward; run cmdUpdate WITH { force: true, yes: true } and assert the user's prior content is preserved at <path>.bak exactly once while the file is refreshed. Run cmdMigrate on the v2 manifest and assert it is a no-op (manifest unchanged). uninstall and assert carve files removed + .bak restored + manifest gone.
    Test B (atomicity): build an artifact set that the auditor will REJECT (e.g. a hook artifact containing a secret/over-permission pattern the auditor flags, or stub the generated set so audit returns an ERROR). Capture the on-disk files + manifest bytes before; invoke the update path (or cmdInstall with the bad set, whichever the lifecycle write uses); assert it returns non-zero and that the files + manifest are byte-identical to before. Reference auditGate behavior — pick an input that deterministically triggers an auditor ERROR per src/auditor.ts rules (read auditor.ts to choose a reliably-flagged pattern).
    Test C (v1 uninstall non-destructive): install via install(), then overwrite carve-manifest.json on disk with a v1-shaped manifest (schemaVersion absent, files: string[] of the same paths) using fs.writeFileSync raw JSON. Call uninstall(root) and assert every listed file is removed, no exception thrown, and readManifest returns null afterward.
    No `any`, no `!`. Use assert/strict.
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness && node --test test/e2e/lifecycle.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test/e2e/lifecycle.e2e.test.ts exists and `node --test test/e2e/lifecycle.e2e.test.ts` exits 0.
    - It asserts: user-modified file survives update (no-force), .bak-once on forced overwrite, migrate no-op on v2, induced audit failure leaves prior install+manifest byte-identical, v1 manifest uninstall removes all files without throwing.
  </acceptance_criteria>
  <done>Lifecycle roundtrip + atomicity + v1 non-destructive uninstall all pass end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: phase verification gate (full suite + coverage + tsc)</name>
  <files>(verification only — runs npm scripts)</files>
  <read_first>
    - package.json scripts (check, test, test:cov)
    - CLAUDE.md (verification gates: npm run check, npm test, coverage >=80)
  </read_first>
  <action>
    Run the full phase verification gate. `npm run check` (tsc --noEmit clean). `npm test` (full unit + e2e suite green). `npm run test:cov` and confirm coverage >= 80 (must not regress from ~95). If coverage on any new function (classify branches, cmdUpdate paths, migrateManifest, normalizeManifest) falls short, add targeted unit assertions in the existing test files (test/unit/manifest.test.ts, test/unit/cli.test.ts, test/e2e/lifecycle.e2e.test.ts) — do NOT create new files. Confirm a migrated carve-manifest.json round-trips through JSON.parse (valid JSON). This task is the LIFE quality-gate closure for the phase.
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness && npm run check && npm test && npm run test:cov</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0.
    - `npm test` exits 0 (all suites pass).
    - `npm run test:cov` reports coverage >= 80 (no regression).
  </acceptance_criteria>
  <done>tsc clean, full suite green, coverage >=80 — M8 quality gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → command dispatch | user-supplied argv selects a mutating command; flags (--force/--yes) gate destructive behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Elevation of Privilege | update --force flag wiring | mitigate | --force is forwarded explicitly to cmdUpdate opts; default (no flag) never overwrites user-modified assets (D6) |
| T-01-13 | Tampering | lifecycle atomicity regressions | mitigate | e2e Test B proves an induced audit failure leaves prior install + manifest byte-identical (C7/C5) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (only node:* + @clack already present). |
</threat_model>

<verification>
- `npm run check` exits 0.
- `npm test` exits 0.
- `npm run test:cov` coverage >= 80.
</verification>

<success_criteria>
- Three lifecycle commands dispatch with correct flag forwarding; USAGE/KNOWN_COMMANDS updated.
- Full lifecycle roundtrip + atomicity + v1 non-destructive uninstall verified e2e.
- Phase quality gate (tsc clean, suite green, coverage >=80) satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-m8-lifecycle-foundation/01-05-SUMMARY.md` when done. This SUMMARY closes M8 — note coverage delta and any limitations carried (migrate back-fill).
</output>
