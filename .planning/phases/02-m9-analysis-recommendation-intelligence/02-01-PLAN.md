---
phase: 02-m9-analysis-recommendation-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/analyzer.ts
  - test/unit/analyzer.test.ts
  - test/unit/designer.test.ts
  - test/unit/wizard.test.ts
  - test/unit/auditor.test.ts
  - test/unit/generator.test.ts
  - test/unit/claudebase.test.ts
  - test/fixtures/monorepo/pnpm-workspace.yaml
  - test/fixtures/monorepo/package.json
  - test/fixtures/docker/Dockerfile
  - test/fixtures/docker/docker-compose.yml
  - test/fixtures/docker/Makefile
autonomous: true
requirements: [INTEL-01, INTEL-02]
must_haves:
  truths:
    - "On a monorepo fixture, ProjectProfile.workspaces[] is populated with detected workspace tool tags"
    - "On a single-package fixture, ProjectProfile.workspaces[] is an empty array"
    - "On a fixture with Dockerfile/docker-compose/Makefile, ProjectProfile.container booleans are set true"
    - "All existing analyze() outputs (type/languages/packageManager/ci) are unchanged"
  artifacts:
    - path: "src/types.ts"
      provides: "ProjectProfile.workspaces and ProjectProfile.container fields"
      contains: "workspaces"
    - path: "src/analyzer.ts"
      provides: "detectWorkspaces + detectContainer read-only detection"
      contains: "detectWorkspaces"
    - path: "test/fixtures/monorepo/pnpm-workspace.yaml"
      provides: "monorepo detection fixture"
    - path: "test/fixtures/docker/Dockerfile"
      provides: "container detection fixture"
  key_links:
    - from: "src/analyzer.ts"
      to: "src/types.ts"
      via: "analyze() returns ProjectProfile with workspaces+container set"
      pattern: "workspaces"
---

<objective>
Extend ProjectProfile with monorepo workspace signals (INTEL-01) and container/build signals (INTEL-02), and add read-only detection in the analyzer that always populates them with deterministic defaults.

Purpose: carve's recommendations must reflect real-world project shape. These two new signal groups are the raw input that the designer (Plan 02) will weight on. Without them the weighting step has nothing to read.

Output:
- `ProjectProfile.workspaces: string[]` and `ProjectProfile.container: { dockerfile, compose, makefile }` (REQUIRED fields, deterministic defaults).
- `detectWorkspaces(root)` and `detectContainer(root)` in analyzer.ts (read-only, append human-readable `signals[]` entries).
- New fixtures `test/fixtures/monorepo/` and `test/fixtures/docker/`.
- All existing ProjectProfile construction sites (analyzer + 5 test helpers) updated so the existing test suite stays green.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.claude/rules/code-style.md
@.claude/rules/project-structure.md

<read_first>
- src/types.ts — ProjectProfile interface (extend here)
- src/analyzer.ts — read-only heuristic scan (add detection here; mirror existing existsSync/readIf patterns)
- test/unit/analyzer.test.ts — fixture + withTempProject patterns (add new tests here)
- test/unit/designer.test.ts (profile() helper at lines 8-14)
- test/unit/wizard.test.ts (profile() helper at lines 12-17)
- test/unit/auditor.test.ts (profile() helper around lines 10-13)
- test/unit/generator.test.ts (profile() helper around lines 10-13)
- test/unit/claudebase.test.ts (profile() helper around lines 12-15)
- test/fixtures/web/ and test/fixtures/cli/ — fixture layout convention (plain files written under fixture dir)
</read_first>

<interfaces>
<!-- Current ProjectProfile (src/types.ts). Add fields; do NOT remove or reorder existing ones. -->
export interface ProjectProfile {
  root: string;
  type: ProjectType;
  languages: string[];
  packageManager: string | null;
  testCmd: string | null;
  lintCmd: string | null;
  formatCmd: string | null;
  ci: string | null;
  hasGit: boolean;
  signals: string[];
}

<!-- analyzer.ts already has: readIf(root, name), existsSync/join from node:fs/node:path, and analyze() builds and returns the ProjectProfile literal at the end. -->

<!-- 5 test helpers build inline ProjectProfile base literals with `...over` spread.
     Each base literal MUST gain `workspaces: []` and `container: { dockerfile: false, compose: false, makefile: false }`
     or TypeScript will error (strict, no missing required props). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ProjectProfile and update all construction sites</name>
  <files>src/types.ts, test/unit/designer.test.ts, test/unit/wizard.test.ts, test/unit/auditor.test.ts, test/unit/generator.test.ts, test/unit/claudebase.test.ts</files>
  <behavior>
    - After this task, `npm run check` (tsc --noEmit) is clean — no missing-property errors.
    - The 5 test-helper `profile()` base literals each include the two new fields with default values.
    - Existing tests still pass (no behavior change — the new fields default to empty/false everywhere).
  </behavior>
  <action>
    In src/types.ts, add two REQUIRED fields to the ProjectProfile interface, immediately after `signals: string[]` (per INTEL-01, INTEL-02):
    - `workspaces: string[]` with a Korean doc comment: monorepo 워크스페이스 도구 태그 (예: ['pnpm-workspace','turbo']). 비-monorepo면 빈 배열.
    - `container: { dockerfile: boolean; compose: boolean; makefile: boolean }` with a Korean doc comment: 컨테이너·빌드 시그널.
    Define the container shape inline in the interface (no separate exported type needed — keep minimal per code-style "단순함이 먼저다").

    Then update the 5 test helper `profile()` base literals (designer/wizard/auditor/generator/claudebase test files). In each base object literal that lists `signals: []`, append `workspaces: []` and `container: { dockerfile: false, compose: false, makefile: false }`. Keep the trailing `...over` spread so per-test overrides still work. Do NOT touch any other line in those test files (surgical change per CLAUDE.md "외과적 변경").

    Note: Korean comments and Korean strings per project convention. No `any`, no non-null `!`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>tsc --noEmit is clean; src/types.ts has `workspaces` and `container` fields; all 5 test helpers compile with the new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read-only detectWorkspaces + detectContainer to analyzer and wire into analyze()</name>
  <files>src/analyzer.ts, test/unit/analyzer.test.ts, test/fixtures/monorepo/pnpm-workspace.yaml, test/fixtures/monorepo/package.json, test/fixtures/docker/Dockerfile, test/fixtures/docker/docker-compose.yml, test/fixtures/docker/Makefile</files>
  <behavior>
    - analyze(test/fixtures/monorepo) → workspaces[] contains 'pnpm-workspace' (and 'npm-workspaces' if package.json has a workspaces field); workspaces is non-empty.
    - analyze(test/fixtures/cli) → workspaces is [] (single-package fixture).
    - analyze(test/fixtures/docker) → container.dockerfile === true, container.compose === true, container.makefile === true.
    - analyze(test/fixtures/cli) → container is all-false.
    - signals[] gains human-readable entries for each detected workspace/container signal.
  </behavior>
  <action>
    Create fixtures first (plain files written under the fixture dirs, mirroring test/fixtures/web layout):
    - test/fixtures/monorepo/pnpm-workspace.yaml: a minimal `packages:\n  - 'packages/*'\n`.
    - test/fixtures/monorepo/package.json: `{ "name": "mono", "private": true, "workspaces": ["packages/*"] }` (npm/yarn workspaces field — exercises that branch too).
    - test/fixtures/docker/Dockerfile: a minimal `FROM node:22-alpine\n`.
    - test/fixtures/docker/docker-compose.yml: a minimal `services:\n  app:\n    build: .\n`.
    - test/fixtures/docker/Makefile: a minimal `build:\n\tnpm run build\n` (real TAB indent).

    In src/analyzer.ts, add two pure helpers (read-only — only existsSync/readIf, never write), mirroring the existing detectPackageManager/detectLanguages style:
    - `detectWorkspaces(root): string[]` — scan and push tags: 'pnpm-workspace' if pnpm-workspace.yaml exists; 'turbo' if turbo.json; 'nx' if nx.json; 'lerna' if lerna.json; 'cargo-workspace' if Cargo.toml exists AND its content contains a `[workspace]` section (use readIf + `.includes('[workspace]')`); 'npm-workspaces' if package.json has a truthy `workspaces` field (reuse the already-parsed `pkg` — add a `workspaces?` key to the PackageJson interface and check it). Return the collected tags (empty array when none).
    - `detectContainer(root): { dockerfile: boolean; compose: boolean; makefile: boolean }` — dockerfile = Dockerfile exists; compose = docker-compose.yml OR docker-compose.yaml exists; makefile = Makefile exists.

    In analyze(): call both (passing `root` and, for detectWorkspaces, the parsed `pkg`), assign results to local consts, push a human-readable Korean signal for each true/non-empty result into `signals` (e.g. `monorepo: pnpm-workspace`, `container: Dockerfile`), and add `workspaces` and `container` to the returned ProjectProfile literal. Keep analyze() read-only.

    In test/unit/analyzer.test.ts, add tests using the existing FIXTURES path + withTempProject helpers:
    - monorepo fixture → workspaces includes 'pnpm-workspace', workspaces.length > 0.
    - cli fixture → workspaces deepEqual [].
    - docker fixture → container all-true.
    - cli fixture → container all-false.
    - a withTempProject Cargo.toml containing `[workspace]` → workspaces includes 'cargo-workspace'.
    Korean comments. No `any`/non-null `!`.
  </action>
  <verify>
    <automated>npm run check && npm test</automated>
  </verify>
  <done>analyzer.ts exports/has detectWorkspaces and detectContainer; analyze() populates both fields; new analyzer tests pass; full suite green (existing 152 tests unaffected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target filesystem → analyzer | analyzer reads arbitrary target-project files (untrusted content) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | detectWorkspaces/detectContainer reading target files | accept | Read-only existsSync/readIf only; never executes or writes target files; malformed content falls through to defaults (empty array / false). |
| T-02-02 | Denial of Service | reading a huge Cargo.toml/package.json | accept | Single-file reads of known small config files; same risk profile as existing readIf usage (no regression). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (stdlib node:fs/node:path only, per D4). No legitimacy gate needed. |
</threat_model>

<verification>
- `npm run check` clean (tsc --noEmit, strict, no `any`).
- `npm test` green — new analyzer tests pass AND all pre-existing tests pass (proves backward compat of the ProjectProfile additions).
- analyzer remains read-only (no writeFileSync/mkdirSync/rmSync introduced in src/analyzer.ts).
</verification>

<success_criteria>
- ROADMAP Success Criteria #1: monorepo fixture → workspaces[] populated; single-package fixture → empty array. (INTEL-01)
- ROADMAP Success Criteria #2: Dockerfile/compose/Makefile fixture → container signals set. (INTEL-02)
- No regression: existing suite passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-m9-analysis-recommendation-intelligence/02-01-SUMMARY.md` when done.
</output>
