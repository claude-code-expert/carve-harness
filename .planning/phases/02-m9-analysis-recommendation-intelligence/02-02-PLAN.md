---
phase: 02-m9-analysis-recommendation-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/designer.ts
  - src/catalog.ts
  - test/unit/designer.test.ts
autonomous: true
requirements: [INTEL-03]
must_haves:
  truths:
    - "For a monorepo+CI profile the designer recommends a higher-coordination slot set than for a single-package profile"
    - "applySignalWeights is a pure, independently-testable function"
    - "Single-package fixtures get no coordination boost (existing designer behavior preserved)"
    - "Coordination components are only added when they exist in `available` (forType)"
  artifacts:
    - path: "src/designer.ts"
      provides: "applySignalWeights pure helper + integration into design()"
      contains: "applySignalWeights"
  key_links:
    - from: "src/designer.ts"
      to: "src/types.ts"
      via: "reads profile.workspaces and profile.ci"
      pattern: "workspaces"
---

<objective>
Add deterministic signal-weighted scoring to the designer (INTEL-03): when a project shows monorepo and/or CI signals, boost coordination components (`parallel-agents`, `coordinator`) into the recommended set — producing a measurably different, higher-coordination slot set than a single-package project.

Purpose: ROADMAP Success Criteria #3 — the designer must recommend a measurably different (higher-coordination) slot set for monorepo+CI vs single-package, via a testable pure function. This consumes the `workspaces`/`ci` signals produced in Plan 01.

Output:
- Exported pure helper `applySignalWeights(profile, baseRecommended, available): string[]`.
- `design()` applies it as an ADDITIVE step on top of the existing recommendation build (core logic NOT rewritten).
- Rationale entries explaining the boost.
- Optional minimal catalog meta ONLY if it makes the boost clearly simpler (decision documented below — default: keep boost in designer with explicit ids).
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/carve-harness/carve-harness/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.claude/rules/code-style.md

<read_first>
- src/designer.ts — design() builds `rec` set + rationale; add the weighting step AFTER the existing build, BEFORE the return
- src/catalog.ts — CatalogComponent shape; 'parallel-agents' and 'coordinator' already exist in CATALOG (applicable: 'all')
- test/unit/designer.test.ts — profile() helper (now includes workspaces/container after Plan 01) + existing design() assertions that MUST still pass
- src/types.ts — ProjectProfile now has workspaces: string[] and ci: string | null (from Plan 02-01)
</read_first>

<interfaces>
<!-- Current design() return (src/designer.ts) — preserve shape exactly: -->
export interface HarnessDesign {
  level: HarnessLevel;
  recommended: string[];
  available: string[];
  rationale: string[];
}

<!-- design(p, levelOverride?) currently: builds `rec` Set, then returns { level, recommended: [...rec], available: available.map(c=>c.id), rationale }. -->

<!-- catalog.ts already contains these ids (applicable: 'all', core:false):
       { id: 'parallel-agents', kind: 'skill', ... }
       { id: 'coordinator', kind: 'skill', ... }
     So forType(p.type) includes them for every type. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add applySignalWeights pure helper and integrate into design()</name>
  <files>src/designer.ts, test/unit/designer.test.ts</files>
  <behavior>
    - applySignalWeights(profile, base, available): given workspaces non-empty OR ci !== null, returns base PLUS 'parallel-agents' and 'coordinator' — but ONLY ids that are present in `available`. No duplicates.
    - Given workspaces=[] AND ci=null, returns base unchanged (same membership).
    - design() on a monorepo+CI profile → recommended includes 'parallel-agents' and 'coordinator'.
    - design() on a single-package profile (workspaces=[], ci=null) → recommended does NOT include 'parallel-agents'/'coordinator' (unless already present via level=full path — see note).
    - Existing designer tests still pass (minimal cli, standard web, full, recommended ⊆ available, auto-commit never recommended).
  </behavior>
  <action>
    In src/designer.ts, add an exported pure function:
    `applySignalWeights(p: ProjectProfile, baseRecommended: string[], available: string[]): string[]`.
    Logic (deterministic, per INTEL-03):
    - Start from a Set built from baseRecommended.
    - Compute `isMonorepo = p.workspaces.length > 0` and `hasCi = p.ci !== null`.
    - If `isMonorepo || hasCi`: for each coordination id in `['parallel-agents','coordinator']`, add it to the set IF `available.includes(id)`.
    - Return the set as an array (membership-stable; order not asserted by tests — use [...set]).
    Keep it a pure function: no I/O, no mutation of inputs (build a new Set).

    In design(): after the existing `rec` build and rationale pushes, BEFORE the return, call applySignalWeights with `[...rec]` and the available id list, assign the result to a new `recommended` array, and use it in the returned object. When the boost actually adds something (compare lengths or membership), push Korean rationale entries, e.g.:
    - monorepo present: `monorepo 시그널 — 병렬·조율 에이전트 가중`
    - ci present: `CI 시그널 — 조율 컴포넌트 가중`
    Only push the rationale that applies (check isMonorepo / hasCi individually). Do NOT alter the existing rationale pushes or the core recommendation logic.

    Catalog meta decision (DOCUMENT in the SUMMARY, do NOT over-build): keep the boost in designer with the explicit id list `['parallel-agents','coordinator']`. Rationale: only two ids, data-driven `boostOn?` meta in catalog.ts would add a field + a filter pass for no simplification (violates code-style "투기적인 건 넣지 않는다" / PROJECT C11 "open the slot only, no over-design"). Therefore catalog.ts is NOT modified in this plan. If during implementation a clearly-simpler data-driven form emerges, add an OPTIONAL `boostOn?: ('monorepo'|'ci')[]` field to CatalogComponent and drive the boost from it — but only if it is strictly simpler, and note the choice in the SUMMARY.

    In test/unit/designer.test.ts, add tests:
    - Unit: applySignalWeights with a monorepo profile (workspaces:['pnpm-workspace'], ci:null) + a base without coordination ids + available including them → result includes both; with workspaces:[],ci:null → result equals base membership.
    - Unit: applySignalWeights does not add an id absent from `available`.
    - Integration: design(profile({ type:'web', workspaces:['turbo'], ci:'github-actions', languages:['ts','js'] })) → recommended includes 'parallel-agents' and 'coordinator'.
    - Integration: design(profile({ type:'web' })) (single-package, ci:null, workspaces:[]) → recommended does NOT include 'parallel-agents'.
    Korean comments. No `any`/non-null `!`. Note: the `recommended ⊆ available` existing test must still hold — the boost only adds ids guaranteed to be in available.
  </action>
  <verify>
    <automated>npm run check && npm test</automated>
  </verify>
  <done>designer.ts exports applySignalWeights; design() applies the additive boost; monorepo+CI profile yields a strictly larger/higher-coordination recommended set than single-package; all existing + new designer tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyzer profile → designer | designer consumes profile signals; no external/untrusted execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | applySignalWeights output feeding installer | accept | Pure function; only adds ids already in `available` (forType-derived from catalog); cannot inject unknown components. Selection gate (D3) still required before install. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (stdlib only, per D4). No legitimacy gate needed. |
</threat_model>

<verification>
- `npm run check` clean.
- `npm test` green — new designer tests pass; existing designer/wizard/generator tests pass unchanged.
- Determinism: applySignalWeights has no I/O; same input → same output.
</verification>

<success_criteria>
- ROADMAP Success Criteria #3: monorepo+CI profile → measurably different (higher-coordination) slot set than single-package, via a testable pure function. (INTEL-03)
- Core designer behavior (level heuristic, core/anti-slop/hook recommendation) preserved.
</success_criteria>

<output>
Create `.planning/phases/02-m9-analysis-recommendation-intelligence/02-02-SUMMARY.md` when done. Document the catalog-meta decision (kept in designer, catalog.ts unmodified, and why).
</output>
