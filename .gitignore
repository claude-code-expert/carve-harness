logs/

# macOS Finder cruft
.DS_Store

# external skill-manager lockfile (theme-factory is vendored directly, not lock-managed)
skills-lock.json

# secrets — never commit env files (HYG-02)
/.env
/.env.*
*.env

# runtime handoff snapshot (regenerated by session-handoff.sh)
specs/HANDOFF.md

# golden-set run evidence (per-assert verdicts + respondent output, regenerated by /eval)
specs/eval-runs/

# install-time local state
.claude/bin/
.claude/harness-manifest.txt
.claude/settings.local.json

# 오펜 정본 문서(타 제품 내부 자료) — 데모 근거로만 로컬 보관, 공개 Pages에 올리지 않음
docs/html/open-demo/
